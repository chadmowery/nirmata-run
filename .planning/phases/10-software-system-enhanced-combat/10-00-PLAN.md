---
phase: 10-software-system-enhanced-combat
plan: 00
type: execute
wave: 1
depends_on: []
files_modified:
  - src/game/systems/software.test.ts
  - src/game/systems/combat.test.ts
  - src/shared/pipeline.test.ts
autonomous: true
requirements: [SOFT-01, SOFT-02, SOFT-04, SOFT-05, SOFT-06, SOFT-07]

must_haves:
  truths:
    - "Test scaffold files exist BEFORE any implementation tasks run"
    - "All describe blocks and test stubs are in place for downstream plans to fill in"
  artifacts:
    - path: "src/game/systems/software.test.ts"
      provides: "Wave 0 test scaffolds for Software burn mechanic, stacking, effects, rarity"
      min_lines: 60
    - path: "src/game/systems/combat.test.ts"
      provides: "Extended with damage pipeline modifier test stubs"
      contains: "resolveDamage"
    - path: "src/shared/pipeline.test.ts"
      provides: "Extended/created with Software death clearing test stubs"
      contains: "Software death clearing"
  key_links: []
---

<objective>
Create Wave 0 test scaffolds for all Phase 10 test files. These empty-but-structured test files must exist BEFORE implementation plans run, ensuring every task's `<verify>` command references a pre-existing test file (Nyquist compliance).

Purpose: Satisfy the Nyquist principle -- test files exist before the code they verify, enabling red-green-refactor workflow in Plans 01-03.
Output: 3 test files with describe blocks and pending/stub tests covering all Phase 10 requirements.
</objective>

<execution_context>
@/Users/cmowery/dev/chadspace/nirmata-run-main/.claude/get-shit-done/workflows/execute-plan.md
@/Users/cmowery/dev/chadspace/nirmata-run-main/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/10-software-system-enhanced-combat/10-CONTEXT.md
@.planning/phases/10-software-system-enhanced-combat/10-VALIDATION.md

<interfaces>
<!-- Existing test patterns to follow -->

From src/game/systems/combat.test.ts (lines 1-10):
```typescript
import { describe, it, expect, vi, beforeEach } from 'vitest';
import { World } from '@engine/ecs/world';
import { Grid } from '@engine/grid/grid';
import { EventBus } from '@engine/events/event-bus';
import { EntityFactory } from '@engine/entity/factory';
import { createCombatSystem } from './combat';
import { Attack, Defense, LootTable, Health, Position, Heat } from '@shared/components';
import { GameplayEvents } from '@shared/events/types';
import { ComponentRegistry } from '@engine/entity/types';
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Wave 0 Test Scaffolds for Phase 10</name>
  <files>
    src/game/systems/software.test.ts,
    src/game/systems/combat.test.ts,
    src/shared/pipeline.test.ts
  </files>
  <read_first>
    src/game/systems/combat.test.ts,
    src/shared/pipeline.ts
  </read_first>
  <action>
    **Create `src/game/systems/software.test.ts`:**
    New file with describe blocks and `it.todo()` stubs covering all Software test requirements from VALIDATION.md. Use Vitest `it.todo()` for stubs so the file is valid and runnable (todos are skipped, not failures).

    Structure:
    ```typescript
    import { describe, it, expect, vi, beforeEach } from 'vitest';

    describe('Software System', () => {
      describe('SoftwareDef Component', () => {
        it.todo('validates valid SoftwareDef schema');
        it.todo('rejects targetSlot values other than weapon or armor');
      });

      describe('BurnedSoftware Component', () => {
        it.todo('validates BurnedSoftware schema with weapon and armor nullable');
      });

      describe('RarityTier Component', () => {
        it.todo('validates RarityTier schema');
        it.todo('RARITY_SCALE_FACTORS matches D-07 (v0.x=1, v1.x=1.5, v2.x=2, v3.x=3)');
      });

      describe('RunInventoryRegistry', () => {
        it.todo('getOrCreate returns inventory with maxSlots=5');
        it.todo('addSoftware rejects when inventory is full (5 items)');
        it.todo('clear empties the inventory');
        it.todo('removeSoftware removes by index and shifts remaining');
      });

      describe('BURN_SOFTWARE Action', () => {
        it.todo('burn with valid Software and matching targetSlot succeeds');
        it.todo('burn with wrong targetSlot is rejected');
        it.todo('burn with duplicate Software type already active is rejected');
        it.todo('overwrite existing Software on same slot destroys old entity');
      });

      describe('Death and Extraction Clearing', () => {
        it.todo('ENTITY_DIED clears BurnedSoftware weapon and armor to null');
        it.todo('ENTITY_DIED clears RunInventory');
        it.todo('Extraction transfers RunInventory items to stash');
      });

      describe('Bleed.exe Effect', () => {
        it.todo('applies BLEED status effect on physical attack hit');
        it.todo('Bleed v2.x scales magnitude to 4 (baseMagnitude=2 * 2.0)');
        it.todo('no status effect when attacker has no Bleed burned');
      });

      describe('Auto-Loader.msi Effect', () => {
        it.todo('checkAutoLoader returns true when Auto-Loader is burned');
        it.todo('checkAutoLoader returns false when no Auto-Loader burned');
      });

      describe('Vampire.exe Effect', () => {
        it.todo('heals player on kill with rarity-scaled amount');
        it.todo('Vampire v3.x heals 15 HP (5 * 3.0)');
        it.todo('heal does not exceed Health.max');
        it.todo('triggers on ENTITY_DIED when killerId is the player');
      });

      describe('Stacking Rules', () => {
        it.todo('no duplicate Software types across all slots (D-09)');
        it.todo('different Software types on different slots fire independently (D-10)');
      });

      describe('Rarity Scaling', () => {
        it.todo('v0.x magnitude = baseMagnitude * 1.0');
        it.todo('v1.x magnitude = baseMagnitude * 1.5');
        it.todo('v2.x magnitude = baseMagnitude * 2.0');
        it.todo('v3.x magnitude = baseMagnitude * 3.0');
      });
    });
    ```

    **Extend `src/game/systems/combat.test.ts`:**
    Add a new describe block at the end of the existing file (INSIDE the outer describe, or as a sibling top-level describe) for the damage pipeline modifier pattern:
    ```typescript
    describe('Damage Pipeline (Phase 10)', () => {
      it.todo('resolveDamage with no modifiers returns base - defense');
      it.todo('resolveDamage with additive pre_defense modifier increases damage');
      it.todo('resolveDamage with multiple modifiers applies all additively');
      it.todo('resolveDamage minimum damage is 1');
      it.todo('collectDamageModifiers reads BurnedSoftware weapon slot');
      it.todo('collectDamageModifiers returns empty when no BurnedSoftware');
      it.todo('venting defender has effectiveArmor=0 (existing behavior preserved)');
    });
    ```

    **Create or extend `src/shared/pipeline.test.ts`:**
    If the file does not exist, create it. If it exists, extend it. Add a describe block for Software death clearing:
    ```typescript
    import { describe, it, expect, vi, beforeEach } from 'vitest';

    describe('Pipeline - Software Integration', () => {
      describe('Software death clearing', () => {
        it.todo('ENTITY_DIED clears BurnedSoftware component on dead entity');
        it.todo('ENTITY_DIED clears RunInventory for session');
      });
    });
    ```

    All stubs use `it.todo()` which Vitest treats as skipped tests (not failures). This ensures `npm run test -- {file} --run` exits 0 for all three files immediately.
  </action>
  <verify>
    <automated>npm run test -- src/game/systems/software.test.ts src/game/systems/combat.test.ts src/shared/pipeline.test.ts --run 2>&1 | tail -5</automated>
  </verify>
  <done>
    - src/game/systems/software.test.ts exists with describe blocks and it.todo() stubs for all SOFT requirements
    - src/game/systems/combat.test.ts extended with damage pipeline describe block and it.todo() stubs
    - src/shared/pipeline.test.ts exists with Software death clearing describe block and it.todo() stubs
    - All three files pass (exit 0) with todos shown as skipped
  </done>
</task>

</tasks>

<verification>
- `npm run test -- src/game/systems/software.test.ts --run` exits 0 (all todos skipped)
- `npm run test -- src/game/systems/combat.test.ts --run` exits 0 (existing tests pass, new todos skipped)
- `npm run test -- src/shared/pipeline.test.ts --run` exits 0 (todos skipped)
- `grep -c "it.todo" src/game/systems/software.test.ts` returns 20+ stubs
</verification>

<success_criteria>
- All 3 test files exist and are runnable before any implementation plan executes
- Stubs cover all requirements from VALIDATION.md Wave 0 checklist
- No test failures (todos are skipped, not red)
</success_criteria>

<output>
After completion, create `.planning/phases/10-software-system-enhanced-combat/10-00-SUMMARY.md`
</output>
