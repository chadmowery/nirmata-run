---
phase: 11-enemy-hierarchy
plan: 04
type: execute
wave: 3
depends_on: ["11-01", "11-02", "11-03"]
files_modified:
  - src/game/entities/templates/null-pointer.json
  - src/game/entities/templates/buffer-overflow.json
  - src/game/entities/templates/fragmenter.json
  - src/game/entities/templates/logic-leaker.json
  - src/game/entities/templates/system-admin.json
  - src/game/entities/templates/seed-eater.json
  - src/game/entities/templates/spawn-tables/depth-distribution.json
  - src/game/generation/entity-placement.ts
  - src/rendering/filters/glitch-effects.ts
  - src/rendering/filters/death-effects.ts
  - src/rendering/animations.ts
  - src/rendering/render-system.ts
autonomous: true
requirements: [ENEMY-01, ENEMY-02, ENEMY-09, ENEMY-10, ENEMY-11]

must_haves:
  truths:
    - "All 6 enemy types defined in JSON entity templates with behaviorType, stats, and loot tables"
    - "Enemies display persistent glitch visual effects (RGB split, displacement) matching the glitch aesthetic"
    - "Each enemy type has a unique death effect (not generic fade-out)"
    - "Enemies leak visual static/distortion on damage hit instead of just a red tint"
    - "Depth-based spawn table controls which enemy types appear at which floor depth"
    - "Enemy count per room scales with depth: 1-2 on floors 1-5, 2-3 on 5-10, 2-4 on 10-15"
  artifacts:
    - path: "src/game/entities/templates/null-pointer.json"
      provides: "Null-Pointer entity template"
      contains: "null_pointer"
    - path: "src/game/entities/templates/system-admin.json"
      provides: "System_Admin entity template WITHOUT health component"
    - path: "src/game/entities/templates/spawn-tables/depth-distribution.json"
      provides: "Depth-range to weighted template mapping"
      contains: "depthRange"
    - path: "src/game/generation/entity-placement.ts"
      provides: "Depth-aware placement reading from spawn table"
      contains: "depthDistribution"
    - path: "src/rendering/filters/glitch-effects.ts"
      provides: "Per-enemy persistent glitch filters and damage distortion"
      contains: "applyPersistentGlitch"
    - path: "src/rendering/filters/death-effects.ts"
      provides: "Per-type unique death animations"
      contains: "queueTypedDeathAnimation"
  key_links:
    - from: "src/game/generation/entity-placement.ts"
      to: "src/game/entities/templates/spawn-tables/depth-distribution.json"
      via: "JSON import for spawn table"
      pattern: "depth-distribution"
    - from: "src/rendering/render-system.ts"
      to: "src/rendering/filters/glitch-effects.ts"
      via: "applyPersistentGlitch called on entity creation"
      pattern: "applyPersistentGlitch"
    - from: "src/rendering/render-system.ts"
      to: "src/rendering/filters/death-effects.ts"
      via: "queueTypedDeathAnimation called on entity death"
      pattern: "queueTypedDeathAnimation"
---

<objective>
Create all 6 enemy JSON entity templates, depth-based spawn distribution, and the full visual effects pipeline (persistent glitch filters, damage distortion, per-type death effects) using pixi-filters.

Purpose: Completes the enemy hierarchy with data definitions, world integration, and visual identity — enemies are fully functional, visually distinct, and spawn appropriately by floor depth.
Output: 6 JSON templates, spawn table, depth-aware placement, pixi-filters glitch rendering, 6 unique death effects.
</objective>

<execution_context>
@/Users/cmowery/dev/chadspace/nirmata-run-main/.claude/get-shit-done/workflows/execute-plan.md
@/Users/cmowery/dev/chadspace/nirmata-run-main/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/11-enemy-hierarchy/11-CONTEXT.md
@.planning/phases/11-enemy-hierarchy/11-RESEARCH.md
@.planning/phases/11-enemy-hierarchy/11-01-SUMMARY.md
@.planning/phases/11-enemy-hierarchy/11-02-SUMMARY.md
@.planning/phases/11-enemy-hierarchy/11-03-SUMMARY.md

<interfaces>
From src/game/entities/templates/goblin.json (existing template pattern):
```json
{
  "name": "goblin",
  "mixins": ["physical", "combatant"],
  "components": {
    "actor": { "isPlayer": false },
    "energy": { "current": 0, "speed": 80, "threshold": 1000 },
    "sprite": { "key": "enemy_triangle" },
    "hostile": {},
    "aiState": { "behavior": "idle", "sightRadius": 6 },
    "fovAwareness": { "canSeePlayer": false },
    "blocksMovement": {},
    "lootTable": { "drops": [...] }
  },
  "overrides": {
    "health": { "current": 8, "max": 8 },
    "attack": { "power": 3 },
    "defense": { "armor": 0 }
  }
}
```

From src/game/generation/entity-placement.ts:
```typescript
export interface PlacementConfig {
  enemiesPerRoom: { min: number; max: number };
  itemsPerRoom: { min: number; max: number };
  enemyTemplates: string[];
  itemTemplates: string[];
  playerOverrides?: Record<string, Record<string, unknown>>;
}
export function placeEntities<T>(world, grid, factory, componentRegistry, rooms, spawnRoom, rng, config?): PlacementResult
```

From src/rendering/sprite-map.ts:
```typescript
export function createEntitySprite(entityId: EntityId, spriteKey: string, container: Container): Sprite;
export function getEntitySprite(entityId: EntityId): Sprite | undefined;
```

From src/rendering/animations.ts:
```typescript
export function queueDeathAnimation(entityId: EntityId, onComplete: () => void): void;
// Currently just fade-out. Death effects plan extends this.
```

From src/rendering/render-system.ts:
```typescript
// handleEntityCreated: creates sprite from SpriteComponent.key
// handleEntityDestroyed: queues death animation then destroys sprite
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: JSON Entity Templates, Spawn Table, and Depth-Aware Placement</name>
  <files>src/game/entities/templates/null-pointer.json, src/game/entities/templates/buffer-overflow.json, src/game/entities/templates/fragmenter.json, src/game/entities/templates/logic-leaker.json, src/game/entities/templates/system-admin.json, src/game/entities/templates/seed-eater.json, src/game/entities/templates/spawn-tables/depth-distribution.json, src/game/generation/entity-placement.ts</files>
  <read_first>src/game/entities/templates/goblin.json, src/game/entities/templates/mixins/physical.json, src/game/entities/templates/mixins/combatant.json, src/game/generation/entity-placement.ts, src/shared/components/ai-state.ts, src/shared/components/pack-member.ts, src/shared/components/corruption-state.ts, src/shared/components/loot-table.ts</read_first>
  <action>
1. **Create 6 enemy JSON templates** (per D-11, ENEMY-11). All follow goblin.json pattern with mixins `["physical", "combatant"]` except system-admin which omits combatant:

   **null-pointer.json** (Tier 1 — teleport flanker):
   ```json
   { "name": "null-pointer", "mixins": ["physical", "combatant"], "components": {
     "actor": { "isPlayer": false },
     "energy": { "current": 0, "speed": 120, "threshold": 1000 },
     "sprite": { "key": "enemy_null_pointer" },
     "hostile": {},
     "aiState": { "behavior": "idle", "sightRadius": 8, "behaviorType": "null_pointer" },
     "fovAwareness": { "canSeePlayer": false },
     "blocksMovement": {},
     "statusEffects": { "effects": [] },
     "lootTable": { "drops": [
       { "template": "health_potion", "chance": 0.25 },
       { "template": "bleed-v0", "chance": 0.15 }
     ] }
   }, "overrides": { "health": { "current": 6, "max": 6 }, "attack": { "power": 4 }, "defense": { "armor": 0 } } }
   ```

   **buffer-overflow.json** (Tier 1 — pack swarm):
   ```json
   { "name": "buffer-overflow", "mixins": ["physical", "combatant"], "components": {
     "actor": { "isPlayer": false },
     "energy": { "current": 0, "speed": 80, "threshold": 1000 },
     "sprite": { "key": "enemy_buffer_overflow" },
     "hostile": {},
     "aiState": { "behavior": "idle", "sightRadius": 6, "behaviorType": "buffer_overflow" },
     "fovAwareness": { "canSeePlayer": false },
     "blocksMovement": {},
     "packMember": { "packId": "", "isLeader": false },
     "statusEffects": { "effects": [] },
     "lootTable": { "drops": [
       { "template": "health_potion", "chance": 0.1 },
       { "template": "auto-loader-v0", "chance": 0.08 }
     ] }
   }, "overrides": { "health": { "current": 3, "max": 3 }, "attack": { "power": 2 }, "defense": { "armor": 0 } } }
   ```
   Note: packId and isLeader are overridden at spawn time by entity-placement.ts.

   **fragmenter.json** (Tier 2 — Dead Zone creator):
   ```json
   { "name": "fragmenter", "mixins": ["physical", "combatant"], "components": {
     "actor": { "isPlayer": false },
     "energy": { "current": 0, "speed": 60, "threshold": 1000 },
     "sprite": { "key": "enemy_fragmenter" },
     "hostile": {},
     "aiState": { "behavior": "idle", "sightRadius": 5, "behaviorType": "fragmenter" },
     "fovAwareness": { "canSeePlayer": false },
     "blocksMovement": {},
     "statusEffects": { "effects": [] },
     "lootTable": { "drops": [
       { "template": "bleed-v1", "chance": 0.2 },
       { "template": "vampire-v1", "chance": 0.15 },
       { "template": "auto-loader-v1", "chance": 0.15 }
     ] }
   }, "overrides": { "health": { "current": 18, "max": 18 }, "attack": { "power": 6 }, "defense": { "armor": 2 } } }
   ```

   **logic-leaker.json** (Tier 2 — ranged kiter):
   ```json
   { "name": "logic-leaker", "mixins": ["physical", "combatant"], "components": {
     "actor": { "isPlayer": false },
     "energy": { "current": 0, "speed": 90, "threshold": 1000 },
     "sprite": { "key": "enemy_logic_leaker" },
     "hostile": {},
     "aiState": { "behavior": "idle", "sightRadius": 7, "behaviorType": "logic_leaker" },
     "fovAwareness": { "canSeePlayer": false },
     "blocksMovement": {},
     "statusEffects": { "effects": [] },
     "lootTable": { "drops": [
       { "template": "auto-loader-v1", "chance": 0.2 },
       { "template": "vampire-v1", "chance": 0.1 }
     ] }
   }, "overrides": { "health": { "current": 12, "max": 12 }, "attack": { "power": 5 }, "defense": { "armor": 1 } } }
   ```

   **system-admin.json** (Tier 3 — invulnerable stalker, per D-16 NO Health):
   ```json
   { "name": "system-admin", "mixins": ["physical"], "components": {
     "actor": { "isPlayer": false },
     "energy": { "current": 0, "speed": 35, "threshold": 1000 },
     "sprite": { "key": "enemy_system_admin" },
     "hostile": {},
     "aiState": { "behavior": "idle", "sightRadius": 20, "behaviorType": "system_admin" },
     "fovAwareness": { "canSeePlayer": false },
     "blocksMovement": {},
     "statusEffects": { "effects": [] }
   } }
   ```
   NO overrides section (no health, attack, or defense). NO combatant mixin. NO lootTable.

   **seed-eater.json** (Tier 3 — tile corruptor, per D-23 killable with high HP):
   ```json
   { "name": "seed-eater", "mixins": ["physical", "combatant"], "components": {
     "actor": { "isPlayer": false },
     "energy": { "current": 0, "speed": 70, "threshold": 1000 },
     "sprite": { "key": "enemy_seed_eater" },
     "hostile": {},
     "aiState": { "behavior": "idle", "sightRadius": 6, "behaviorType": "seed_eater" },
     "fovAwareness": { "canSeePlayer": false },
     "blocksMovement": {},
     "corruptionState": { "corruptionWave": 1, "corruptedTileKeys": [], "tilesPerTurn": 2, "spawnFrequency": 3, "turnsSinceLastSpawn": 0 },
     "statusEffects": { "effects": [] },
     "lootTable": { "drops": [
       { "template": "bleed-v2", "chance": 0.4 },
       { "template": "vampire-v2", "chance": 0.3 },
       { "template": "auto-loader-v2", "chance": 0.3 }
     ] }
   }, "overrides": { "health": { "current": 40, "max": 40 }, "attack": { "power": 7 }, "defense": { "armor": 3 } } }
   ```

2. **Create spawn-tables directory and depth-distribution.json** (per D-14, D-15, ENEMY-09):
   - Create directory: `src/game/entities/templates/spawn-tables/`
   - File: `depth-distribution.json` with structure:
   ```json
   {
     "tables": [
       {
         "depthRange": { "min": 1, "max": 4 },
         "enemiesPerRoom": { "min": 1, "max": 2 },
         "templates": [
           { "name": "null-pointer", "weight": 50 },
           { "name": "buffer-overflow", "weight": 30, "isPack": true, "packSize": { "min": 5, "max": 8 } },
           { "name": "goblin", "weight": 20 }
         ]
       },
       {
         "depthRange": { "min": 5, "max": 9 },
         "enemiesPerRoom": { "min": 2, "max": 3 },
         "templates": [
           { "name": "null-pointer", "weight": 25 },
           { "name": "buffer-overflow", "weight": 15, "isPack": true, "packSize": { "min": 5, "max": 8 } },
           { "name": "fragmenter", "weight": 30 },
           { "name": "logic-leaker", "weight": 25 },
           { "name": "goblin", "weight": 5 }
         ]
       },
       {
         "depthRange": { "min": 10, "max": 15 },
         "enemiesPerRoom": { "min": 2, "max": 4 },
         "templates": [
           { "name": "fragmenter", "weight": 25 },
           { "name": "logic-leaker", "weight": 25 },
           { "name": "null-pointer", "weight": 15 },
           { "name": "buffer-overflow", "weight": 10, "isPack": true, "packSize": { "min": 5, "max": 8 } },
           { "name": "system-admin", "weight": 5, "maxPerFloor": 1, "minDistanceFromPlayer": 10 },
           { "name": "seed-eater", "weight": 10, "maxPerFloor": 1 }
         ]
       }
     ]
   }
   ```

3. **Refactor entity-placement.ts** for depth-based spawning (per D-14):
   - Import the spawn table JSON: `import depthDistribution from '../entities/templates/spawn-tables/depth-distribution.json'`
   - Add `depth?: number` field to `PlacementConfig` interface (default 1)
   - Add `SpawnTableEntry` interface: `{ name: string; weight: number; isPack?: boolean; packSize?: { min: number; max: number }; maxPerFloor?: number; minDistanceFromPlayer?: number }`
   - Add `DepthTable` interface: `{ depthRange: { min: number; max: number }; enemiesPerRoom: { min: number; max: number }; templates: SpawnTableEntry[] }`
   - Add `getTableForDepth(depth: number): DepthTable` function — finds first table where depth is within range
   - Add `selectWeightedTemplate(templates: SpawnTableEntry[], rng): SpawnTableEntry` function — weighted random selection
   - In `placeEntities`: if `config.depth` is set, use `getTableForDepth` to get the appropriate spawn table. Override `enemiesPerRoom` and `enemyTemplates` from the table.
   - Handle pack spawning: if selected template has `isPack: true`, spawn a full pack in the room (per D-06). Generate unique `packId` from room coordinates. Override `packMember` component on each spawned entity: `{ packId: "pack-{roomX}-{roomY}", isLeader: i === 0 }`.
   - Handle `maxPerFloor` constraints: track how many of each template have been spawned across all rooms. Skip if at max.
   - Handle `minDistanceFromPlayer`: for System_Admin per Pitfall 3, check Manhattan distance from spawn room center. Skip if too close.
   - Keep backward compatibility: if `depth` is not set, use the existing flat `enemyTemplates` array behavior.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - src/game/entities/templates/null-pointer.json contains `"behaviorType": "null_pointer"` and `"speed": 120`
    - src/game/entities/templates/buffer-overflow.json contains `"behaviorType": "buffer_overflow"` and `"packMember"` component
    - src/game/entities/templates/fragmenter.json contains `"behaviorType": "fragmenter"` and `"speed": 60`
    - src/game/entities/templates/logic-leaker.json contains `"behaviorType": "logic_leaker"` and `"speed": 90`
    - src/game/entities/templates/system-admin.json contains `"behaviorType": "system_admin"` and `"speed": 35` and does NOT contain `"health"` or `"lootTable"`
    - src/game/entities/templates/seed-eater.json contains `"behaviorType": "seed_eater"` and `"corruptionState"` component and `"max": 40` HP
    - src/game/entities/templates/spawn-tables/depth-distribution.json contains 3 table entries with depthRange, enemiesPerRoom, and templates arrays
    - src/game/generation/entity-placement.ts contains `depth` field in PlacementConfig
    - src/game/generation/entity-placement.ts contains `getTableForDepth` function
    - src/game/generation/entity-placement.ts contains `selectWeightedTemplate` function
    - src/game/generation/entity-placement.ts contains pack spawning logic with `packId` generation
    - src/game/generation/entity-placement.ts contains `maxPerFloor` constraint check
    - src/game/generation/entity-placement.ts contains `minDistanceFromPlayer` check (>= 10 for System_Admin)
    - `npx tsc --noEmit` exits with code 0
  </acceptance_criteria>
  <done>All 6 enemy JSON templates created following goblin.json pattern, depth-distribution spawn table maps 3 depth bands to weighted templates, entity-placement reads spawn table based on depth with pack spawning, maxPerFloor, and minDistanceFromPlayer constraints</done>
</task>

<task type="auto">
  <name>Task 2: Glitch Visual Effects, Damage Distortion, and Per-Type Death Effects</name>
  <files>src/rendering/filters/glitch-effects.ts, src/rendering/filters/death-effects.ts, src/rendering/animations.ts, src/rendering/render-system.ts</files>
  <read_first>src/rendering/render-system.ts, src/rendering/sprite-map.ts, src/rendering/animations.ts, src/shared/components/ai-state.ts, src/shared/events/types.ts, src/rendering/constants.ts</read_first>
  <action>
1. **Install pixi-filters** (run in action):
   ```bash
   npm install pixi-filters@6.1.5
   ```

2. **Create glitch-effects.ts** (`src/rendering/filters/glitch-effects.ts`) per D-11, ENEMY-01, ENEMY-02:
   - Import from pixi-filters: `GlitchFilter`, `RGBSplitFilter`
   - Import from pixi.js: `Sprite`
   - Import `AIBehaviorType` from `@shared/components/ai-state`

   **`applyPersistentGlitch(sprite: Sprite, behaviorType: AIBehaviorType): void`** — per ENEMY-01:
   - Switch on behaviorType:
     - `NULL_POINTER`: `RGBSplitFilter` with red=[1,0], green=[0,1], blue=[0,0] — subtle chromatic aberration
     - `BUFFER_OVERFLOW`: `RGBSplitFilter` with red=[0.5,0], green=[0,0.5], blue=[0,0] — mild split
     - `FRAGMENTER`: `GlitchFilter` with slices=3, offset=5, direction=0, fillMode=0 — subtle horizontal tear
     - `LOGIC_LEAKER`: `RGBSplitFilter` with red=[1.5,0], green=[0,1.5], blue=[0,0] — more pronounced split
     - `SYSTEM_ADMIN`: `GlitchFilter` with slices=8, offset=10, direction=0, fillMode=0 — heavy static
     - `SEED_EATER`: `GlitchFilter` with slices=5, offset=8, direction=0, fillMode=0 + `RGBSplitFilter` — combo
     - `BASIC` / default: `RGBSplitFilter` with red=[0.3,0], green=[0,0.3], blue=[0,0] — very light
   - Apply filters: `sprite.filters = [filter1, filter2, ...]`
   - Per Pitfall 5: limit to max 2 filters per sprite for performance

   **`applyDamageDistortion(sprite: Sprite, duration: number = 200): void`** — per D-11, ENEMY-02:
   - Create temporary `GlitchFilter` with slices=10, offset=30, direction=0 (heavy horizontal tear)
   - Save existing filters. Set `sprite.filters = [...existingFilters, glitch]`
   - Use `setTimeout` to remove the damage filter after `duration` ms, restoring original filters
   - Guard: check `!sprite.destroyed` before removal

   **`removePersistentGlitch(sprite: Sprite): void`**:
   - Set `sprite.filters = null` — cleanup for when entity is destroyed

3. **Create death-effects.ts** (`src/rendering/filters/death-effects.ts`) per D-12, D-13, ENEMY-10:
   - Import from pixi-filters: `GlitchFilter`, `RGBSplitFilter`
   - Import from pixi.js: `Sprite`
   - Import `AIBehaviorType` from `@shared/components/ai-state`
   - Import TILE_SIZE from `../constants`

   **`queueTypedDeathAnimation(entityId: EntityId, behaviorType: AIBehaviorType, sprite: Sprite, onComplete: () => void): void`**:
   - Switch on behaviorType for per-type death effects (per D-12):

     - **NULL_POINTER** — "Flicker and vanish": rapidly toggle sprite.visible (true/false) 6 times over 400ms, then fade alpha to 0 over 200ms. Total duration 600ms.

     - **BUFFER_OVERFLOW** — "Pixel shrapnel explosion": scale sprite to 1.5x over 150ms while reducing alpha. Apply heavy RGBSplitFilter (red=[10,0], green=[0,10]) for chromatic explosion effect. Fade to 0 alpha over next 150ms. Total 300ms.

     - **FRAGMENTER** — "Crumble dissolution": apply heavy GlitchFilter (slices=20, offset=50). Over 500ms, increase filter offset from 50 to 200 while fading alpha. Looks like the sprite is being torn apart horizontally.

     - **LOGIC_LEAKER** — "Static dissolution": apply GlitchFilter with increasing slices. Start at slices=5, ramp to slices=30 over 400ms. Simultaneously fade alpha and scale down to 0.5. Total 400ms.

     - **SYSTEM_ADMIN** — Should not die (no Health), but defensive: heavy glitch flash + fade for 300ms if somehow triggered.

     - **SEED_EATER** — "Corruption collapse": heavy GlitchFilter + RGBSplit. Over 600ms, increase both filter intensities while the sprite rotates slightly (rotation += 0.2 radians) and scales down to 0.3x. Represents the corruption collapsing.

     - **BASIC** (default goblin) — existing behavior: alpha fade over 300ms.

   - Each animation uses `requestAnimationFrame` loop or integrates with the existing tween system by pushing to `activeAnimations` in animations.ts.
   - All animations call `onComplete()` when finished.

4. **Extend animations.ts** to support new death animation types:
   - Add new tween types to the `TweenAnimation` type union: `'death-flicker'`, `'death-explode'`, `'death-crumble'`, `'death-static'`, `'death-collapse'`
   - In `tickAnimations`, add handlers for each new type that manipulate sprite properties (alpha, scale, filters, visible) based on progress `t`
   - This is the cleanest integration — reuses the existing frame-by-frame tween system

5. **Update render-system.ts** to integrate glitch effects:
   - Import `applyPersistentGlitch`, `applyDamageDistortion`, `removePersistentGlitch` from `./filters/glitch-effects`
   - Import `queueTypedDeathAnimation` from `./filters/death-effects`
   - Import `AIState`, `AIBehaviorType` from `@shared/components`

   **In handleEntityCreated**: after creating sprite, check if entity has AIState component. If `aiState.behaviorType` exists and is not BASIC, call `applyPersistentGlitch(sprite, aiState.behaviorType)`. Per ENEMY-01.

   **In DAMAGE_DEALT handler** (add new handler): get defender sprite via `getEntitySprite(defenderId)`. If sprite exists and defender has AIState (is an enemy), call `applyDamageDistortion(sprite, 200)`. Per ENEMY-02.

   **In handleEntityDestroyed**: check if entity had AIState component. If so, use `queueTypedDeathAnimation(entityId, behaviorType, sprite, () => destroyEntitySprite(entityId))` instead of the generic `queueDeathAnimation`. Per ENEMY-10. Fall back to generic death animation if no behaviorType.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - src/rendering/filters/glitch-effects.ts contains `applyPersistentGlitch` export with switch on `AIBehaviorType` for all 7 types
    - src/rendering/filters/glitch-effects.ts contains `applyDamageDistortion` export with temporary GlitchFilter and setTimeout removal
    - src/rendering/filters/glitch-effects.ts imports from `pixi-filters` (GlitchFilter, RGBSplitFilter)
    - src/rendering/filters/death-effects.ts contains `queueTypedDeathAnimation` export with switch on all 6 enemy AIBehaviorType values
    - src/rendering/filters/death-effects.ts contains distinct death effects: NULL_POINTER has flicker logic, BUFFER_OVERFLOW has scale+RGB, FRAGMENTER has crumble, LOGIC_LEAKER has static, SEED_EATER has rotation+collapse
    - src/rendering/render-system.ts imports and calls `applyPersistentGlitch` in handleEntityCreated
    - src/rendering/render-system.ts has DAMAGE_DEALT listener that calls `applyDamageDistortion`
    - src/rendering/render-system.ts uses `queueTypedDeathAnimation` in handleEntityDestroyed when entity has AIState
    - `npx tsc --noEmit` exits with code 0
  </acceptance_criteria>
  <done>pixi-filters installed, persistent glitch effects on all enemy sprites, damage causes horizontal tear distortion, each of 6 enemy types has unique death animation, render system integrates all effects on create/damage/death</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes
- `npm test -- --run` all tests pass
- All 6 JSON templates are valid JSON and parseable
- system-admin.json has NO health overrides
- depth-distribution.json has 3 depth bands covering floors 1-15
- entity-placement supports depth parameter and falls back to flat templates without it
- pixi-filters package installed in package.json
</verification>

<success_criteria>
- 6 enemy templates with correct behaviorType, energy speeds, stats, and loot tables matching CONTEXT.md decisions
- System_Admin template has no Health component (per D-16)
- Spawn table distributes Tier 1 on floors 1-5, Tier 2 introduced on 5-10, Tier 3 on 10-15 (per ENEMY-09)
- Persistent glitch effects render on all enemy sprites at creation (per ENEMY-01)
- Damage distortion replaces red tint with horizontal tear/static (per ENEMY-02, D-11)
- 6 unique death effects play instead of generic fade (per ENEMY-10, D-12, D-13)
</success_criteria>

<output>
After completion, create `.planning/phases/11-enemy-hierarchy/11-04-SUMMARY.md`
</output>
