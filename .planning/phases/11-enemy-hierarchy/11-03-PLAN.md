---
phase: 11-enemy-hierarchy
plan: 03
type: execute
wave: 2
depends_on: ["11-01"]
files_modified:
  - src/shared/components/corruption-state.ts
  - src/shared/components/index.ts
  - src/shared/events/types.ts
  - src/game/systems/ai.ts
  - src/game/systems/tile-corruption.ts
  - src/game/systems/run-ender.ts
autonomous: true
requirements: [ENEMY-07, ENEMY-08]

must_haves:
  truths:
    - "System_Admin has no Health component, cannot be killed, only stunned by DISRUPT status effect"
    - "System_Admin instantly ends the run on adjacency contact from either direction"
    - "System_Admin stalks slowly (energy speed 35) always pathfinding toward the player"
    - "Seed_Eater corrupts tiles in a spreading pattern, pushing displaced entities to nearest walkable tile"
    - "Seed_Eater spawns Tier 1 sub-processes (Null-Pointer or Buffer-Overflow) from existing templates"
    - "Seed_Eater is killable with high HP and drops high-value loot"
  artifacts:
    - path: "src/shared/components/corruption-state.ts"
      provides: "Corruption tracking component for Seed_Eater"
      contains: "defineComponent('corruptionState'"
      exports: ["CorruptionState"]
    - path: "src/game/systems/tile-corruption.ts"
      provides: "Tile corruption spread system"
      contains: "createTileCorruptionSystem"
    - path: "src/game/systems/run-ender.ts"
      provides: "System_Admin adjacency-based run end detection"
      contains: "createRunEnderSystem"
  key_links:
    - from: "src/game/systems/ai.ts"
      to: "src/game/systems/run-ender.ts"
      via: "System_Admin handler checks for adjacency after movement"
      pattern: "RUN_ENDED"
    - from: "src/game/systems/tile-corruption.ts"
      to: "src/engine/grid/grid.ts"
      via: "grid tile type mutations for corruption spread"
      pattern: "setTileType\\|isWalkable"
---

<objective>
Implement Tier 3 "Logic Breakers" enemies: System_Admin (invulnerable slow stalker with instant run-end on touch) and Seed_Eater (tile corruptor that spawns sub-processes).

Purpose: Tier 3 enemies create the highest tension on deep floors (10-15), with System_Admin being an existential threat to avoid and Seed_Eater as a mini-boss that rewards aggression.
Output: CorruptionState component, tile-corruption system, run-ender system, System_Admin and Seed_Eater AI handlers.
</objective>

<execution_context>
@/Users/cmowery/dev/chadspace/nirmata-run-main/.claude/get-shit-done/workflows/execute-plan.md
@/Users/cmowery/dev/chadspace/nirmata-run-main/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/11-enemy-hierarchy/11-CONTEXT.md
@.planning/phases/11-enemy-hierarchy/11-RESEARCH.md
@.planning/phases/11-enemy-hierarchy/11-01-SUMMARY.md

<interfaces>
From src/shared/components/ai-state.ts (extended in 11-01):
```typescript
export enum AIBehaviorType {
  BASIC = 'basic', NULL_POINTER = 'null_pointer', BUFFER_OVERFLOW = 'buffer_overflow',
  FRAGMENTER = 'fragmenter', LOGIC_LEAKER = 'logic_leaker',
  SYSTEM_ADMIN = 'system_admin', SEED_EATER = 'seed_eater',
}
```

From src/game/systems/ai.ts (refactored in 11-01):
```typescript
// processEnemyTurn has switch(ai.behaviorType) dispatch
// processSystemAdminTurn and processSeedEaterTurn are stubs — replace them
// findPlayerEntity(), computeEnemyFov(), pathfindToward() available
// createAISystem takes: world, grid, movementSystem, eventBus
```

From src/shared/events/types.ts (extended in 11-01):
```typescript
RUN_ENDED: { reason: string; entityId: EntityId };
ENEMY_TELEPORTED: { entityId: EntityId; fromX: number; fromY: number; toX: number; toY: number };
```

From src/engine/ecs/world.ts:
```typescript
// world.createEntity(), world.addComponent(), world.getComponent(), world.destroyEntity()
// world.query(Component1, Component2) — returns EntityId[]
```

From src/engine/entity/factory.ts:
```typescript
// entityFactory.create(world, templateName, componentRegistry, overrides) — creates entity from JSON template
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: CorruptionState Component, Run-Ender System, and Events</name>
  <files>src/shared/components/corruption-state.ts, src/shared/components/index.ts, src/shared/events/types.ts, src/game/systems/run-ender.ts</files>
  <read_first>src/shared/components/index.ts, src/shared/events/types.ts, src/shared/components/status-effects.ts, src/game/systems/ai.ts, src/engine/grid/grid.ts</read_first>
  <action>
1. **Create CorruptionState component** (`src/shared/components/corruption-state.ts`):
   - `defineComponent('corruptionState', z.object({ corruptionWave: z.number().int().default(1), corruptedTileKeys: z.array(z.string()).default([]), tilesPerTurn: z.number().int().default(2), spawnFrequency: z.number().int().default(3), turnsSinceLastSpawn: z.number().int().default(0) }))`
   - `corruptedTileKeys` stores "x,y" strings of already-corrupted tiles
   - `tilesPerTurn`: how many tiles corrupt per Seed_Eater turn (default 2)
   - `spawnFrequency`: spawn a sub-process every N turns (default every 3 turns)
   - Export `CorruptionState` and `CorruptionStateData`

2. **Register component** (`src/shared/components/index.ts`):
   - Import CorruptionState from `./corruption-state`
   - Add to COMPONENTS_REGISTRY array
   - Add `export * from './corruption-state'`

3. **Add events** (`src/shared/events/types.ts`):
   - `TILE_CORRUPTED: { x: number; y: number; newType: 'wall' | 'floor'; corruptorId: EntityId }`
   - `ENTITY_DISPLACED: { entityId: EntityId; fromX: number; fromY: number; toX: number; toY: number }`
   - `SUB_PROCESS_SPAWNED: { parentId: EntityId; childId: EntityId; templateName: string }`
   - `ADMIN_DETECTED: { floorId?: number }` (for ambient HUD warning per D-19)

4. **Create run-ender system** (`src/game/systems/run-ender.ts`):
   - Factory: `createRunEnderSystem<T extends GameplayEvents>(world: World<T>, grid: Grid, eventBus: EventBus<T>)`
   - Per D-17: Check adjacency bidirectionally
   - Listen to `ENTITY_MOVED` event: after any entity moves, check if it has `AIBehaviorType.SYSTEM_ADMIN`. If so, check if the moved entity is now adjacent to or on the same tile as the player. If yes, emit `RUN_ENDED` with `{ reason: 'FATAL: ADMIN_CONTACT', entityId: playerId }`.
   - Also: when player moves (check Actor.isPlayer), check if the destination tile has a System_Admin entity. If so, emit `RUN_ENDED`.
   - Emit `MESSAGE_EMITTED` with `{ text: 'FATAL: System_Admin made contact. Run terminated.', type: 'error' }`
   - Return `{ init(), dispose() }`
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - src/shared/components/corruption-state.ts contains `defineComponent('corruptionState'` with `corruptionWave`, `corruptedTileKeys`, `tilesPerTurn`, `spawnFrequency`, `turnsSinceLastSpawn` fields
    - src/shared/components/index.ts contains `CorruptionState` in COMPONENTS_REGISTRY array
    - src/shared/events/types.ts contains `TILE_CORRUPTED:`, `ENTITY_DISPLACED:`, `SUB_PROCESS_SPAWNED:`, and `ADMIN_DETECTED:`
    - src/game/systems/run-ender.ts contains `createRunEnderSystem` export
    - src/game/systems/run-ender.ts checks for `AIBehaviorType.SYSTEM_ADMIN` on entity move
    - src/game/systems/run-ender.ts emits `RUN_ENDED` with reason `'FATAL: ADMIN_CONTACT'`
    - src/game/systems/run-ender.ts handles both directions: System_Admin moving to player AND player moving to System_Admin (per D-17)
    - `npx tsc --noEmit` exits with code 0
  </acceptance_criteria>
  <done>CorruptionState component registered, run-ender system detects bidirectional adjacency and triggers instant run-end, new events added for corruption/displacement/spawning/admin detection</done>
</task>

<task type="auto">
  <name>Task 2: System_Admin and Seed_Eater AI Handlers + Tile Corruption System</name>
  <files>src/game/systems/ai.ts, src/game/systems/tile-corruption.ts</files>
  <read_first>src/game/systems/ai.ts, src/game/systems/run-ender.ts, src/shared/components/corruption-state.ts, src/shared/components/ai-state.ts, src/shared/components/status-effects.ts, src/shared/events/types.ts, src/engine/grid/grid.ts, src/engine/entity/factory.ts</read_first>
  <action>
1. **Create tile-corruption system** (`src/game/systems/tile-corruption.ts`):
   - Factory: `createTileCorruptionSystem<T extends GameplayEvents>(world: World<T>, grid: Grid, eventBus: EventBus<T>, entityFactory: EntityFactory, componentRegistry: ComponentRegistry)`
   - `spreadCorruption(seedEaterId: EntityId)` method:
     - Get CorruptionState and Position for the Seed_Eater
     - Get adjacent tiles at current corruption wave radius (BFS ring pattern outward from Seed_Eater's position)
     - Filter to tiles not already corrupted (check corruptedTileKeys)
     - Select up to `tilesPerTurn` (default 2) random candidates from the ring
     - Per D-20: flip each tile type — walkable becomes wall, wall becomes walkable
     - Per D-21: if a tile becomes wall and has entities on it (check `grid.getEntitiesAt(x, y)`), push each entity to the nearest walkable tile. Skip the Seed_Eater itself (immune). Calculate nearest walkable via BFS from corrupted tile. Update entity's Position component, call `grid.removeEntity` + `grid.addEntity`. Emit `ENTITY_DISPLACED` event.
     - Safety check per Pitfall 2: before corrupting a tile to wall, verify the player would NOT be trapped (player still has at least 1 adjacent walkable tile after corruption). If corruption would trap player, skip that tile.
     - Add corrupted tile keys to CorruptionState
     - Emit `TILE_CORRUPTED` per tile
     - Increment corruptionWave
   - `spawnSubProcess(seedEaterId: EntityId)` method per D-22:
     - Randomly pick 'null-pointer' or 'buffer-overflow' template (50/50)
     - Find a walkable tile within 3 tiles of the Seed_Eater
     - Create entity via `entityFactory.create(world, templateName, componentRegistry, { position: { x, y } })`
     - Add to grid
     - Emit `SUB_PROCESS_SPAWNED` event
     - Emit `MESSAGE_EMITTED`: "Seed_Eater spawned a sub-process!"
   - `tick()` method: for each Seed_Eater entity (query CorruptionState + Position), call `spreadCorruption()`. Increment `turnsSinceLastSpawn`. If `turnsSinceLastSpawn >= spawnFrequency`, call `spawnSubProcess()` and reset counter.
   - Return `{ init(), dispose(), tick() }`

2. **System_Admin handler** (replace stub in `src/game/systems/ai.ts`) per D-16/D-17/D-18/D-19:
   - `processSystemAdminTurn(entityId)`:
     - Check for DISRUPT status effect on this entity. If active, skip turn (stunned). Decrement is handled by status effect system.
     - If not stunned: always pathfind toward player using `pathfindToward()`. Move one step via `movementSystem.processMove()`.
     - After movement, the run-ender system handles adjacency detection (no need to check here)
     - Emit `ADMIN_DETECTED` event on first turn (for HUD warning per D-19)
   - System_Admin has NO Health component — the JSON template (plan 11-04) will omit it. Combat system's resolveBumpAttack will exit early when `defenderHealth` is null (already guards for this).
   - Stats: sightRadius=20 (always knows where player is effectively), energy speed=35 (per D-18 — very slow, ~1 turn per 3 player turns)

3. **Seed_Eater handler** (replace stub in `src/game/systems/ai.ts`) per D-20/D-21/D-22/D-23:
   - `processSeedEaterTurn(entityId)`:
     - Get CorruptionState component (must exist)
     - The tile-corruption system's `tick()` handles the corruption spread and sub-process spawning
     - For actual movement: if player visible AND adjacent, bump attack (Seed_Eater is a tanky melee mini-boss per D-23)
     - If player visible AND not adjacent, pathfind toward player
     - If player not visible: stay in place (ceiling-mounted, doesn't patrol)
   - Stats (discretionary): sightRadius=6, energy speed=70 (moderate speed), HP=40 (mini-boss tier per D-23)
   - Note: the Seed_Eater AI handler handles movement/attack. The tile-corruption system handles corruption spread and spawning separately, triggered per turn.

4. **Update createAISystem** to accept tile-corruption system reference (optional, null if unavailable):
   - Add parameter: `tileCorruptionSystem: { tick: () => void } | null`
   - The tile-corruption system tick should be called from the game loop, not from within the AI system. So the AI handler for Seed_Eater only handles movement/attack. Document this in a code comment.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - src/game/systems/tile-corruption.ts contains `createTileCorruptionSystem` export
    - src/game/systems/tile-corruption.ts contains `spreadCorruption` method with BFS ring pattern
    - src/game/systems/tile-corruption.ts contains player-trap safety check before corrupting tile to wall
    - src/game/systems/tile-corruption.ts contains `spawnSubProcess` that calls `entityFactory.create` with 'null-pointer' or 'buffer-overflow' template
    - src/game/systems/tile-corruption.ts emits `TILE_CORRUPTED`, `ENTITY_DISPLACED`, and `SUB_PROCESS_SPAWNED` events
    - src/game/systems/ai.ts `processSystemAdminTurn` checks for DISRUPT status effect and skips turn if active
    - src/game/systems/ai.ts `processSystemAdminTurn` always pathfinds toward player (no sight range check — System_Admin always knows)
    - src/game/systems/ai.ts `processSystemAdminTurn` emits `ADMIN_DETECTED` event
    - src/game/systems/ai.ts `processSeedEaterTurn` handles movement/attack (not corruption — that's the separate system)
    - `npx tsc --noEmit` exits with code 0
  </acceptance_criteria>
  <done>System_Admin always stalks player slowly with stun check, Seed_Eater corrupts tiles in spreading pattern with entity displacement and spawns Tier 1 sub-processes, tile corruption includes player-trap safety check</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes
- `npm test -- --run` all existing tests pass
- System_Admin has no Health reference — combat system naturally ignores it
- Tile corruption safety check prevents player softlocks
- Sub-process spawning creates real entities from existing templates
</verification>

<success_criteria>
- System_Admin stalks at speed 35, checks for DISRUPT stun, emits ADMIN_DETECTED for HUD warning
- Run-ender system detects bidirectional adjacency and emits RUN_ENDED instantly
- Seed_Eater corrupts 2 tiles/turn in expanding ring, pushes entities to safety, spawns sub-processes every 3 turns
- Tile corruption has safety check preventing player entrapment
</success_criteria>

<output>
After completion, create `.planning/phases/11-enemy-hierarchy/11-03-SUMMARY.md`
</output>
