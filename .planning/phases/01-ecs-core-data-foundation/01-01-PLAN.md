---
phase: 01-ecs-core-data-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - tsconfig.json
  - vitest.config.ts
  - eslint.config.js
  - .prettierrc
  - src/engine/ecs/.gitkeep
  - src/engine/events/.gitkeep
  - src/engine/entity/.gitkeep
  - src/engine/grid/.gitkeep
  - src/engine/index.ts
  - src/game/components/.gitkeep
  - src/game/entities/templates/.gitkeep
  - src/game/events/.gitkeep
autonomous: true
requirements:
  - ARCH-01
  - ARCH-02

must_haves:
  truths:
    - "TypeScript compiles with strict mode and path aliases resolve correctly"
    - "Vitest runs and discovers test files in src/**/*.test.ts"
    - "ESLint rejects imports from game/ inside engine/ with clear error messages"
    - "Prettier formats .ts files without errors"
    - "Directory structure separates engine/ and game/ at the top level of src/"
  artifacts:
    - path: "package.json"
      provides: "Project manifest with all Phase 1 dependencies"
      contains: "vitest"
    - path: "tsconfig.json"
      provides: "TypeScript strict config with path aliases"
      contains: "strict"
    - path: "vitest.config.ts"
      provides: "Vitest configuration with path alias resolution"
      contains: "defineConfig"
    - path: "eslint.config.js"
      provides: "ESLint 9 flat config with import boundary enforcement"
      contains: "no-restricted-paths"
    - path: ".prettierrc"
      provides: "Prettier formatting config"
  key_links:
    - from: "tsconfig.json"
      to: "vitest.config.ts"
      via: "path alias agreement (@engine, @game)"
      pattern: "@engine|@game"
    - from: "eslint.config.js"
      to: "src/engine/**"
      via: "import-x/no-restricted-paths zones"
      pattern: "engine.*game|no-restricted-paths"
---

<objective>
Project scaffolding and architecture enforcement for the roguelike engine.

Purpose: Establish the tooling foundation (TypeScript, Vitest, ESLint, Prettier) and directory structure that all subsequent plans depend on. ESLint boundary rules are enforced from the first commit per ARCH-02.
Output: A buildable, lintable, testable project skeleton with engine/game separation enforced at the lint level.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-ecs-core-data-foundation/01-RESEARCH.md
@.planning/phases/01-ecs-core-data-foundation/01-CONTEXT.md
@.planning/phases/01-ecs-core-data-foundation/01-VALIDATION.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Project initialization and tooling configuration</name>
  <files>package.json, tsconfig.json, vitest.config.ts, .prettierrc, src/engine/ecs/.gitkeep, src/engine/events/.gitkeep, src/engine/entity/.gitkeep, src/engine/grid/.gitkeep, src/engine/index.ts, src/game/components/.gitkeep, src/game/entities/templates/.gitkeep, src/game/events/.gitkeep</files>
  <behavior>
    - `npx tsc --noEmit` exits 0 with no errors
    - `npx vitest run` exits 0 (discovers no tests, but framework loads)
    - Path aliases @engine and @game resolve correctly in both tsc and vitest
  </behavior>
  <action>
    1. Run `npm init -y` to create package.json
    2. Install production deps: `npm install zod`
    3. Install dev deps: `npm install -D typescript@^5.9 vitest@^4.1 eslint@^9 eslint-plugin-import-x prettier@^3`
    4. Run `npx tsc --init` then configure tsconfig.json:
       - `"strict": true`
       - `"target": "ES2022"`, `"module": "ESNext"`, `"moduleResolution": "bundler"`
       - `"outDir": "./dist"`, `"rootDir": "./src"`
       - Path aliases: `"paths": { "@engine/*": ["./src/engine/*"], "@game/*": ["./src/game/*"] }`
       - `"skipLibCheck": true`
    5. Create vitest.config.ts:
       - Use `defineConfig` from vitest/config
       - `test.globals: true`, `test.include: ['src/**/*.test.ts']`
       - `resolve.alias`: `@engine` → `./src/engine`, `@game` → `./src/game`
    6. Create .prettierrc: `{ "singleQuote": true, "trailingComma": "all", "printWidth": 100 }`
    7. Create directory structure with .gitkeep files:
       - src/engine/ecs/, src/engine/events/, src/engine/entity/, src/engine/grid/
       - src/game/components/, src/game/entities/templates/, src/game/events/
    8. Create src/engine/index.ts as empty barrel export: `// Engine public API — populated as modules are built`
    9. Add npm scripts to package.json: `"test": "vitest run"`, `"test:watch": "vitest"`, `"lint": "eslint src/"`, `"typecheck": "tsc --noEmit"`, `"format": "prettier --write src/"`
  </action>
  <verify>
    <automated>npx tsc --noEmit && npx vitest run --reporter=verbose 2>&1 | head -20</automated>
  </verify>
  <done>TypeScript compiles, Vitest loads, path aliases configured, directory structure exists with engine/game separation</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: ESLint 9 flat config with architecture boundary enforcement</name>
  <files>eslint.config.js, src/engine/__tests__/boundary.test.ts</files>
  <behavior>
    - ESLint reports an error when a file in src/engine/ imports from src/game/
    - ESLint reports an error when a file in src/engine/ imports from src/rendering/
    - ESLint reports an error when a file in src/engine/ imports from src/network/
    - ESLint reports an error when a file in src/engine/ imports from src/ui/
    - ESLint passes on valid engine-internal imports
  </behavior>
  <action>
    1. Create eslint.config.js using ESLint 9 flat config format:
       - Import `eslint-plugin-import-x`
       - Configure TypeScript parser: `@typescript-eslint/parser` (install `typescript-eslint` as dev dep if not already present)
       - Define `import-x/no-restricted-paths` rule with zones:
         - target: `./src/engine/**`, from: `./src/game/**` → "Engine must not import from game layer."
         - target: `./src/engine/**`, from: `./src/rendering/**` → "Engine must not import from rendering layer."
         - target: `./src/engine/**`, from: `./src/network/**` → "Engine must not import from network layer."
         - target: `./src/engine/**`, from: `./src/ui/**` → "Engine must not import from UI layer."
       - Set `sourceType: 'module'` and configure import resolver for TypeScript
    2. Install additional dev deps if needed: `npm install -D typescript-eslint @typescript-eslint/parser`
    3. Create a boundary violation test at src/engine/__tests__/boundary.test.ts:
       - Uses `child_process.execSync` to run eslint on a temporary file that violates the boundary
       - Alternatively: create a simple test that runs `npx eslint --no-eslintrc -c eslint.config.js` on a known-good engine file and asserts exit 0
    4. Verify by running `npx eslint src/engine/` — should exit 0 (no violations in skeleton)
  </action>
  <verify>
    <automated>npx eslint src/engine/ && npx vitest run src/engine/__tests__/boundary.test.ts --reporter=verbose</automated>
  </verify>
  <done>ESLint flat config enforces engine/game boundary; lint passes on clean skeleton; boundary test validates that violations would be caught</done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` — TypeScript compiles with zero errors
2. `npx vitest run --reporter=verbose` — Vitest runs (boundary test passes)
3. `npx eslint src/engine/` — ESLint passes on engine directory
4. Directory structure exists: `find src -type d | sort` shows engine/ecs, engine/events, engine/entity, engine/grid, game/components, game/entities/templates, game/events
</verification>

<success_criteria>
- TypeScript strict mode compiles with path aliases
- Vitest discovers and runs tests in src/**/*.test.ts
- ESLint rejects engine→game imports with clear error messages
- Prettier is configured and formats consistently
- Directory skeleton separates engine/ and game/ in src/
</success_criteria>

<output>
After completion, create `.planning/phases/01-ecs-core-data-foundation/01-01-SUMMARY.md`
</output>
