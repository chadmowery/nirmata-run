---
phase: 20-tiered-loot-distribution
plan: 03
type: execute
wave: 2
depends_on: ["20-02"]
files_modified: [
  "src/game/entities/templates/null-pointer.json",
  "src/game/entities/templates/seed-eater.json",
  "src/game/entities/templates/system-admin.json"
]
autonomous: true
requirements: [LMC-01]
must_haves:
  truths:
    - "Defeating higher tier enemies yields progressively better equipment"
  artifacts:
    - path: "src/game/entities/templates/null-pointer.json"
      provides: "Tier 1 drop table"
    - path: "src/game/entities/templates/seed-eater.json"
      provides: "Tier 2 drop table"
    - path: "src/game/entities/templates/system-admin.json"
      provides: "Tier 3 drop table"
  key_links:
    - from: "src/game/entities/templates/system-admin.json"
      to: "LootTable tier"
      via: "JSON property"
      pattern: "\"tier\": 3"
---

<objective>
Update enemy templates to explicitly define their tier and populate their loot tables with the new tiered weapons, armor, and software.

Purpose: Wire up the tiered loot items into the enemies so the reward system can drop them.
Output: Updated `null-pointer.json`, `seed-eater.json`, and `system-admin.json`.
</objective>

<execution_context>
@/Users/chadmowery/personal/dev/dungeon-runner/nimrata-run/.gemini/get-shit-done/workflows/execute-plan.md
@/Users/chadmowery/personal/dev/dungeon-runner/nimrata-run/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/game/entities/templates/null-pointer.json
@src/game/entities/templates/seed-eater.json
@src/game/entities/templates/system-admin.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update Tier 1 Enemies</name>
  <files>src/game/entities/templates/null-pointer.json</files>
  <read_first>
    - src/game/entities/templates/null-pointer.json
  </read_first>
  <action>
    Update `null-pointer.json` (Tier 1 Swarm):
    - Set `components.lootTable.tier` to 1.
    - Append to `components.lootTable.drops`:
      - `{ "template": "rifle-v1", "chance": 0.1 }`
      - `{ "template": "armor-v1", "chance": 0.1 }`
    Keep existing drops intact.
  </action>
  <verify>
    <automated>grep -q '"tier": 1' src/game/entities/templates/null-pointer.json</automated>
  </verify>
  <acceptance_criteria>
    - file `src/game/entities/templates/null-pointer.json` contains `"tier": 1`
    - file `src/game/entities/templates/null-pointer.json` contains `"template": "rifle-v1"`
  </acceptance_criteria>
  <done>Tier 1 enemy correctly defines its tier and drops.</done>
</task>

<task type="auto">
  <name>Task 2: Update Tier 2 Enemies</name>
  <files>src/game/entities/templates/seed-eater.json</files>
  <read_first>
    - src/game/entities/templates/seed-eater.json
  </read_first>
  <action>
    Update `seed-eater.json` (Tier 2 Elite):
    - Set `components.lootTable.tier` to 2.
    - Append to `components.lootTable.drops`:
      - `{ "template": "rifle-v2", "chance": 0.2 }`
      - `{ "template": "armor-v2", "chance": 0.2 }`
    Keep existing drops intact.
  </action>
  <verify>
    <automated>grep -q '"tier": 2' src/game/entities/templates/seed-eater.json</automated>
  </verify>
  <acceptance_criteria>
    - file `src/game/entities/templates/seed-eater.json` contains `"tier": 2`
    - file `src/game/entities/templates/seed-eater.json` contains `"template": "rifle-v2"`
  </acceptance_criteria>
  <done>Tier 2 enemy correctly defines its tier and drops.</done>
</task>

<task type="auto">
  <name>Task 3: Update Tier 3 Enemies</name>
  <files>src/game/entities/templates/system-admin.json</files>
  <read_first>
    - src/game/entities/templates/system-admin.json
  </read_first>
  <action>
    Update `system-admin.json` (Tier 3 Boss):
    - Add the `lootTable` component under `components`.
    - Set `components.lootTable.tier` to 3.
    - Set `components.lootTable.drops` array to:
      - `{ "template": "rifle-v3", "chance": 0.4 }`
      - `{ "template": "armor-v3", "chance": 0.4 }`
      - `{ "template": "bleed-v3", "chance": 0.5 }`
      - `{ "template": "vampire-v3", "chance": 0.5 }`
      - `{ "template": "auto-loader-v3", "chance": 0.5 }`
  </action>
  <verify>
    <automated>grep -q '"tier": 3' src/game/entities/templates/system-admin.json</automated>
  </verify>
  <acceptance_criteria>
    - file `src/game/entities/templates/system-admin.json` contains `"tier": 3`
    - file `src/game/entities/templates/system-admin.json` contains `"template": "rifle-v3"`
  </acceptance_criteria>
  <done>Tier 3 enemy correctly defines its tier and drops.</done>
</task>

</tasks>

<verification>
grep -r '"tier":' src/game/entities/templates/*.json
</verification>

<success_criteria>
Enemy templates include tiered equipment and have tier explicitly defined in their lootTables.
</success_criteria>

<output>
After completion, create `.planning/phases/20-tiered-loot-distribution/20-03-SUMMARY.md`
</output>