---
phase: 20-tiered-loot-distribution
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: ["src/game/systems/reward-drop.test.ts", "src/game/systems/reward-drop.ts"]
autonomous: true
requirements: [LMC-01]
must_haves:
  truths:
    - "Loot drops are fully deterministic per seed"
    - "Players are guaranteed to receive equipment when defeating elite and boss enemies"
  artifacts:
    - path: "src/game/systems/reward-drop.ts"
      provides: "Drop mechanics logic"
    - path: "src/game/systems/reward-drop.test.ts"
      provides: "Tests for drop system"
  key_links:
    - from: "src/game/systems/reward-drop.ts"
      to: "rot-js"
      via: "import RNG"
      pattern: "RNG\\.getUniform"
---

<objective>
Update the reward drop system to use deterministic RNG and implement guaranteed equipment drops for higher-tier enemies.

Purpose: Ensure balanced and deterministic drop rates across dungeon floors, and reward players for defeating elite/boss enemies.
Output: Updated `reward-drop.ts` system and its corresponding test suite.
</objective>

<execution_context>
@/Users/chadmowery/personal/dev/dungeon-runner/nimrata-run/.gemini/get-shit-done/workflows/execute-plan.md
@/Users/chadmowery/personal/dev/dungeon-runner/nimrata-run/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/game/systems/reward-drop.ts
@src/shared/components/loot-table.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add Drop System Tests</name>
  <files>src/game/systems/reward-drop.test.ts</files>
  <read_first>
    - src/game/systems/reward-drop.ts
  </read_first>
  <behavior>
    - Test 1: Should use RNG.getUniform() instead of Math.random() for all drops
    - Test 2: Should guarantee 1 item drop when entity lootTable tier is >= 2
    - Test 3: Should not guarantee an item drop when entity lootTable tier is 1
  </behavior>
  <action>
    Create the test suite for `RewardDropSystem`.
    - Mock `rot-js/lib/rng` with `vi.mock('rot-js/lib/rng')` and export a mock `getUniform` function.
    - Set up a mock `World`, `EventBus`, `Grid`, and `EntityFactory` to pass to `createRewardDropSystem`.
    - Create a test entity with a `Dying` component (reason not pickup), `Position`, and `LootTable`.
    - Assert that `Math.random` is NOT called (use `vi.spyOn(Math, 'random')`).
    - Assert that `RNG.getUniform` is called for chance resolutions.
    - Verify that for a `tier: 2` loot table, the entity factory creates the guaranteed item from the `drops` array.
  </action>
  <verify>
    <automated>npx vitest run src/game/systems/reward-drop.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - file `src/game/systems/reward-drop.test.ts` contains `vi.mock('rot-js/lib/rng')`
    - command `npx vitest run src/game/systems/reward-drop.test.ts` exits 0
  </acceptance_criteria>
  <done>Reward drop system has full test coverage for LMC-01 requirements.</done>
</task>

<task type="auto">
  <name>Task 2: Implement Deterministic & Guaranteed Drops</name>
  <files>src/game/systems/reward-drop.ts</files>
  <read_first>
    - src/game/systems/reward-drop.ts
  </read_first>
  <action>
    Update `src/game/systems/reward-drop.ts`:
    1. Import `RNG` from `rot-js/lib/rng`.
    2. Replace all instances of `Math.random()` with `RNG.getUniform()`.
    3. Modify section "1. Roll for Equipment (LootTable)":
       - Clone the array: `const drops = [...lootTable.drops];`
       - Read the tier: `const tier = lootTable.tier ?? 1;`
       - If `tier >= 2 && drops.length > 0`:
         - Calculate index: `const guaranteedIndex = Math.floor(RNG.getUniform() * drops.length);`
         - Create the entity for `drops[guaranteedIndex].template` and add to grid.
         - Splice it from `drops`: `drops.splice(guaranteedIndex, 1);`
       - Iterate over remaining `drops`:
         - If `RNG.getUniform() < drop.chance`, create the entity and add to grid.
  </action>
  <verify>
    <automated>npx vitest run src/game/systems/reward-drop.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - file `src/game/systems/reward-drop.ts` does NOT contain `Math.random`
    - file `src/game/systems/reward-drop.ts` contains `import RNG from 'rot-js/lib/rng'`
    - file `src/game/systems/reward-drop.ts` contains logic for `tier >= 2` guaranteed drop
    - command `npx vitest run src/game/systems/reward-drop.test.ts` exits 0
  </acceptance_criteria>
  <done>System correctly drops guaranteed items for elite/boss tiers using rot-js RNG.</done>
</task>

</tasks>

<verification>
npx vitest run src/game/systems/reward-drop.test.ts
</verification>

<success_criteria>
Tests pass and reward-drop.ts logic is strictly deterministic.
</success_criteria>

<output>
After completion, create `.planning/phases/20-tiered-loot-distribution/20-01-SUMMARY.md`
</output>