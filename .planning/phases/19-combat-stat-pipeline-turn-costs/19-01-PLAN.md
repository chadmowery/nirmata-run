---
phase: 19-combat-stat-pipeline-turn-costs
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/shared/components/equipment-slots.ts
  - src/shared/components/index.ts
  - src/game/systems/combat.ts
  - src/game/systems/combat.test.ts
autonomous: true
requirements: [LMC-02]
must_haves:
  truths:
    - "D-01: collectDamageModifiers runs on-demand at combat time with no cached derived stat — no permanent stat mutation"
    - "D-02: collectDamageModifiers returns weapon software modifiers via EquipmentSlots hierarchy traversal"
    - "D-03: EquipmentSlots schema has weapon and armor fields only — no additional slots"
    - "D-06: getEffectiveArmor returns base shell armor + equipped armor entity Defense value via hierarchy traversal"
    - "Player base Attack and Defense components are never mutated by equipment"
    - "Null EquipmentSlots.weapon returns zero weapon modifiers gracefully"
  artifacts:
    - path: "src/shared/components/equipment-slots.ts"
      provides: "EquipmentSlots Zod-validated component definition"
      exports: ["EquipmentSlots", "EquipmentSlotsData"]
    - path: "src/shared/components/index.ts"
      provides: "EquipmentSlots registered in COMPONENTS_REGISTRY; BurnedSoftware removed from registry and barrel export"
      contains: "EquipmentSlots"
    - path: "src/game/systems/combat.ts"
      provides: "Hierarchy-traversal collectDamageModifiers and getEffectiveArmor helper"
      contains: "getEffectiveArmor"
    - path: "src/game/systems/combat.test.ts"
      provides: "Tests for EquipmentSlots-based stat pipeline"
      min_lines: 40
  key_links:
    - from: "src/game/systems/combat.ts"
      to: "src/shared/components/equipment-slots.ts"
      via: "import EquipmentSlots"
      pattern: "import.*EquipmentSlots.*from"
    - from: "src/game/systems/combat.ts"
      to: "src/shared/components/index.ts"
      via: "import Children"
      pattern: "import.*Children.*from"
---

<objective>
Create the EquipmentSlots component and rewrite the combat stat pipeline to dynamically calculate damage/armor by traversing the equipment entity hierarchy.

Purpose: Satisfies LMC-02 — effective stats are computed on-demand at combat time by walking EquipmentSlots -> weapon/armor entity -> Children -> SoftwareDef, ensuring zero permanent stat mutation on the player entity.

Output: New EquipmentSlots component, rewritten collectDamageModifiers, new getEffectiveArmor helper, and passing unit tests.
</objective>

<execution_context>
@/Users/cmowery/dev/chadspace/nirmata-run-feature-phase-18/.claude/get-shit-done/workflows/execute-plan.md
@/Users/cmowery/dev/chadspace/nirmata-run-feature-phase-18/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-combat-stat-pipeline-turn-costs/19-PATTERNS.md
@.planning/phases/19-combat-stat-pipeline-turn-costs/19-RESEARCH.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create EquipmentSlots component and register in component index</name>
  <files>src/shared/components/equipment-slots.ts, src/shared/components/index.ts</files>
  <read_first>
    - src/shared/components/burned-software.ts (exact analog to copy structure from)
    - src/shared/components/index.ts (current registry, imports, exports to modify)
    - src/shared/components/defense.ts (secondary analog for minimal component pattern)
  </read_first>
  <behavior>
    - EquipmentSlots component validates { weapon: number | null, armor: number | null } via Zod
    - EquipmentSlots defaults both fields to null
    - COMPONENTS_REGISTRY includes EquipmentSlots
    - BurnedSoftware is removed from COMPONENTS_REGISTRY and barrel export
  </behavior>
  <action>
    Create src/shared/components/equipment-slots.ts following the exact structure of burned-software.ts: import z from 'zod', import defineComponent from '@engine/ecs/types', export const EquipmentSlots = defineComponent('equipmentSlots', z.object({ weapon: z.number().nullable().default(null), armor: z.number().nullable().default(null) })). Export type EquipmentSlotsData.

    In src/shared/components/index.ts:
    - Replace the import of BurnedSoftware from './burned-software' with import of EquipmentSlots from './equipment-slots'
    - Replace the re-export (export * from './burned-software') with (export * from './equipment-slots')
    - In COMPONENTS_REGISTRY array: replace the BurnedSoftware entry with EquipmentSlots

    Per D-02: EquipmentSlots is the traversal anchor for all stat calculations.
    Per D-06: BurnedSoftware is removed from the registry — no shim.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - src/shared/components/equipment-slots.ts exports EquipmentSlots and EquipmentSlotsData
    - EquipmentSlots.key === 'equipmentSlots'
    - equipment-slots.ts schema validates { weapon: null, armor: null } and { weapon: 42, armor: 99 }
    - src/shared/components/index.ts contains "export * from './equipment-slots'"
    - src/shared/components/index.ts does NOT contain "export * from './burned-software'" or "BurnedSoftware" anywhere
    - COMPONENTS_REGISTRY array contains EquipmentSlots (not BurnedSoftware)
  </acceptance_criteria>
  <done>EquipmentSlots component exists, is registered, BurnedSoftware removed from component registry and exports</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Rewrite collectDamageModifiers and add getEffectiveArmor helper</name>
  <files>src/game/systems/combat.ts, src/game/systems/combat.test.ts</files>
  <read_first>
    - src/game/systems/combat.ts (full file — current collectDamageModifiers at ~line 49, effectiveArmor inline at ~lines 154-157, imports at line 6)
    - src/game/systems/combat.test.ts (full file — existing test setup pattern, entity creation, assertion style)
    - src/shared/components/equipment-slots.ts (just created in Task 1 — confirms export name)
    - src/shared/components/children.ts (Children component structure used in traversal)
  </read_first>
  <behavior>
    - collectDamageModifiers with equipped weapon + software children returns additive modifier with value = baseMagnitude * scaleFactor
    - collectDamageModifiers with EquipmentSlots.weapon === null returns empty modifiers (no crash)
    - collectDamageModifiers skips children with effectType 'dot', 'action_economy', or 'heal_on_kill'
    - getEffectiveArmor returns base Defense.armor + equipped armor entity Defense.armor
    - getEffectiveArmor with no equipped armor returns only base Defense.armor
    - getEffectiveArmor returns 0 when isVenting is true (regardless of equipment)
    - Player entity Attack and Defense components remain unchanged after combat resolution with equipment
  </behavior>
  <action>
    In combat.ts imports (line 6): replace BurnedSoftware with EquipmentSlots and add Children to the import from '@shared/components'.

    Replace the BurnedSoftware read block in collectDamageModifiers (lines ~54-70) with the EquipmentSlots hierarchy traversal: read world.getComponent(attackerId, EquipmentSlots), if weapon is non-null, get weaponId, read Children component on weaponId, iterate children?.entityIds, for each childId read SoftwareDef and RarityTier, skip effectTypes 'dot'/'action_economy'/'heal_on_kill', push modifier with source: software:type, type: 'additive', value: baseMagnitude * scaleFactor, phase: 'pre_defense'.

    Add new module-level function getEffectiveArmor(world, defenderId, isVenting): if isVenting return 0; read baseDefense from world.getComponent(defenderId, Defense); read equipmentSlots from world.getComponent(defenderId, EquipmentSlots); if armor slot non-null, read Defense component from armor entity and add to base. Return total.

    Replace inline armor calculation (lines ~154-157) with call to getEffectiveArmor(w, defenderId, defenderHeat?.isVenting ?? false).

    In combat.test.ts: add a new describe block 'EquipmentSlots stat pipeline' with tests covering all behavior items above. Setup pattern: create attacker entity with Attack + EquipmentSlots, create weapon entity, create software child entity with SoftwareDef + RarityTier, add Children component to weapon entity with [softwareChildId]. For armor tests: create defender with Defense + EquipmentSlots, create armor entity with Defense component.
  </action>
  <verify>
    <automated>npx vitest run src/game/systems/combat.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - combat.ts imports EquipmentSlots and Children (not BurnedSoftware)
    - combat.ts contains function getEffectiveArmor
    - collectDamageModifiers reads world.getComponent(attackerId, EquipmentSlots) instead of BurnedSoftware
    - combat.test.ts contains test 'returns weapon software modifiers via EquipmentSlots traversal' (or similar)
    - combat.test.ts contains test 'returns zero modifiers when weapon slot is null'
    - combat.test.ts contains test 'effective armor includes equipped armor Defense'
    - combat.test.ts contains test 'base Attack/Defense not mutated after combat with equipment'
    - npx vitest run src/game/systems/combat.test.ts exits 0
  </acceptance_criteria>
  <done>Stat pipeline reads from EquipmentSlots hierarchy; getEffectiveArmor helper exists; all combat tests pass</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client -> Server API | Action intents cross trust boundary (validated by Zod ActionIntentSchema on server) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-01 | Tampering | EquipmentSlots.weapon/armor EntityId | accept | EntityIds are server-authoritative integers; client cannot forge valid entity references since server reconciles state. Existing pattern for all EntityId-bearing components. |
| T-19-02 | Denial of Service | collectDamageModifiers traversal depth | accept | Maximum depth is 2 (EquipmentSlots -> weapon -> Children -> software). No recursive traversal. Bounded by design. |
| T-19-SC | Tampering | npm installs | accept | No new packages installed in this phase. |
</threat_model>

<verification>
npx vitest run src/game/systems/combat.test.ts — all tests pass
npx tsc --noEmit — no compile errors from EquipmentSlots usage or BurnedSoftware removal from registry
</verification>

<success_criteria>
- EquipmentSlots component defined with Zod validation and registered in COMPONENTS_REGISTRY
- BurnedSoftware removed from component registry and barrel exports
- collectDamageModifiers walks EquipmentSlots.weapon -> Children -> SoftwareDef hierarchy
- getEffectiveArmor computes base + equipped armor entity Defense
- No permanent mutation of player Attack/Defense components
- All combat.test.ts tests pass
</success_criteria>

<output>
Create `.planning/phases/19-combat-stat-pipeline-turn-costs/19-01-SUMMARY.md` when done
</output>
