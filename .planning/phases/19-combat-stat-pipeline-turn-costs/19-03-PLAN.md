---
phase: 19-combat-stat-pipeline-turn-costs
plan: 03
type: execute
wave: 2
depends_on: [19-01]
files_modified:
  - src/game/systems/software-effects.ts
  - src/game/systems/run-ender.ts
  - src/game/ui/sync-bridge.ts
  - src/game/engine-factory.ts
  - src/game/systems/software.test.ts
  - src/shared/components/burned-software.ts
autonomous: true
requirements: [LMC-02, EQP-03]
must_haves:
  truths:
    - "D-06: BurnedSoftware component file is deleted from the codebase — no shim, no dead code"
    - "D-06: All software effect functions (bleed, vampire, auto-loader) read from EquipmentSlots hierarchy instead of BurnedSoftware"
    - "Run-ender clears EquipmentSlots on player death/extraction instead of BurnedSoftware"
    - "Sync-bridge populates UI mods from EquipmentSlots + Children traversal"
    - "Player entity spawns with EquipmentSlots { weapon: null, armor: null } in engine-factory"
    - "TypeScript compiles with zero errors after BurnedSoftware removal"
  artifacts:
    - path: "src/game/systems/software-effects.ts"
      provides: "Migrated bleed/vampire/auto-loader functions using EquipmentSlots traversal"
      contains: "EquipmentSlots"
    - path: "src/game/systems/run-ender.ts"
      provides: "Clears EquipmentSlots on death/extraction"
      contains: "EquipmentSlots"
    - path: "src/game/ui/sync-bridge.ts"
      provides: "UI mods populated from EquipmentSlots hierarchy"
      contains: "EquipmentSlots"
    - path: "src/game/engine-factory.ts"
      provides: "Player entity initialized with EquipmentSlots default"
      contains: "equipmentSlots"
  key_links:
    - from: "src/game/systems/software-effects.ts"
      to: "src/shared/components/equipment-slots.ts"
      via: "import EquipmentSlots for hierarchy traversal"
      pattern: "import.*EquipmentSlots"
    - from: "src/game/engine-factory.ts"
      to: "src/shared/components/equipment-slots.ts"
      via: "playerOverrides equipmentSlots entry"
      pattern: "equipmentSlots.*weapon.*null"
---

<objective>
Complete the BurnedSoftware removal sweep: migrate all remaining consumers to EquipmentSlots + Children traversal, initialize EquipmentSlots on the player entity, delete burned-software.ts, and verify the full codebase compiles cleanly.

Purpose: Completes D-06 (BurnedSoftware removed entirely) and ensures all gameplay systems that previously read BurnedSoftware now work with the Phase 18 entity hierarchy. Also ensures player entity spawns with EquipmentSlots for both the stat pipeline (LMC-02) and equipment actions (EQP-03) to function.

Output: All BurnedSoftware references eliminated, player entity initialized correctly, full test suite passes, tsc --noEmit clean.
</objective>

<execution_context>
@/Users/cmowery/dev/chadspace/nirmata-run-feature-phase-18/.claude/get-shit-done/workflows/execute-plan.md
@/Users/cmowery/dev/chadspace/nirmata-run-feature-phase-18/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-combat-stat-pipeline-turn-costs/19-PATTERNS.md
@.planning/phases/19-combat-stat-pipeline-turn-costs/19-RESEARCH.md
@.planning/phases/19-combat-stat-pipeline-turn-costs/19-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate software-effects.ts, run-ender.ts, and sync-bridge.ts from BurnedSoftware to EquipmentSlots</name>
  <files>src/game/systems/software-effects.ts, src/game/systems/run-ender.ts, src/game/ui/sync-bridge.ts</files>
  <read_first>
    - src/game/systems/software-effects.ts (full file — applyBleedOnHit at ~line 18, applyVampireOnKill, checkAutoLoader functions that read BurnedSoftware)
    - src/game/systems/run-ender.ts (full file — BurnedSoftware patch calls at ~lines 95-97 and 105-107)
    - src/game/ui/sync-bridge.ts (full file — BurnedSoftware read for mods at ~lines 37-54)
    - src/shared/components/children.ts (Children component API for traversal)
  </read_first>
  <action>
    In software-effects.ts:
    - Replace import of BurnedSoftware with EquipmentSlots and Children from '@shared/components'
    - applyBleedOnHit: replace BurnedSoftware.weapon read with EquipmentSlots.weapon -> Children traversal -> find child where SoftwareDef.type === 'bleed'. Pattern: const equipmentSlots = world.getComponent(attackerId, EquipmentSlots); if weapon null return; const children = world.getComponent(equipmentSlots.weapon, Children); iterate children?.entityIds to find bleed software. Keep rest of function logic unchanged.
    - applyVampireOnKill: replace BurnedSoftware.armor read with EquipmentSlots.armor -> Children traversal -> find child where SoftwareDef.type === 'vampire'. Same pattern as bleed but on the armor slot.
    - checkAutoLoader: replace BurnedSoftware.weapon read with EquipmentSlots.weapon -> Children traversal -> find child where SoftwareDef.type === 'auto-loader'.

    In run-ender.ts:
    - Replace import of BurnedSoftware with EquipmentSlots from '@shared/components'
    - Replace both instances of world.hasComponent(playerId, BurnedSoftware) / world.patchComponent(playerId, BurnedSoftware, { weapon: null, armor: null }) with the equivalent using EquipmentSlots: world.hasComponent(playerId, EquipmentSlots) and world.patchComponent(playerId, EquipmentSlots, { weapon: null, armor: null })

    In sync-bridge.ts:
    - Replace import of BurnedSoftware with EquipmentSlots and Children from '@shared/components'
    - Replace the BurnedSoftware read block (burned.weapon / burned.armor -> SoftwareDef.name) with EquipmentSlots traversal: read EquipmentSlots, if weapon non-null, get Children of weapon entity, iterate childIds, read SoftwareDef.name from each child, push to mods. Same for armor slot.
  </action>
  <verify>
    <automated>npx vitest run src/game/systems/software.test.ts src/game/systems/run-ender.test.ts 2>/dev/null; npx tsc --noEmit 2>&1 | grep -c "BurnedSoftware" || echo "0 references"</automated>
  </verify>
  <acceptance_criteria>
    - software-effects.ts does NOT import BurnedSoftware
    - software-effects.ts imports EquipmentSlots and Children
    - software-effects.ts applyBleedOnHit reads from EquipmentSlots.weapon -> Children traversal
    - software-effects.ts applyVampireOnKill reads from EquipmentSlots.armor -> Children traversal
    - software-effects.ts checkAutoLoader reads from EquipmentSlots.weapon -> Children traversal
    - run-ender.ts does NOT import BurnedSoftware
    - run-ender.ts patches EquipmentSlots (not BurnedSoftware) on death/extraction
    - sync-bridge.ts does NOT import BurnedSoftware
    - sync-bridge.ts populates mods array via EquipmentSlots + Children traversal
  </acceptance_criteria>
  <done>All three files migrated from BurnedSoftware to EquipmentSlots; no BurnedSoftware imports remain in game layer</done>
</task>

<task type="auto">
  <name>Task 2: Initialize player EquipmentSlots in engine-factory, migrate software tests, delete burned-software.ts</name>
  <files>src/game/engine-factory.ts, src/game/systems/software.test.ts, src/shared/components/burned-software.ts</files>
  <read_first>
    - src/game/engine-factory.ts (full file — playerOverrides object at ~lines 146-151, BurnedSoftware override at ~lines 193-196, imports)
    - src/game/systems/software.test.ts (full file — any BurnedSoftware setup patterns to migrate to EquipmentSlots + Children)
    - src/shared/components/burned-software.ts (confirm file exists before deletion)
  </read_first>
  <action>
    In engine-factory.ts:
    - Remove BurnedSoftware from imports (if still imported)
    - Add EquipmentSlots default to playerOverrides: 'equipmentSlots': { weapon: null, armor: null }
    - Delete the entire playerOverrides['burnedSoftware'] = { weapon: weaponSoftwareId, armor: armorSoftwareId } block (lines ~193-196). The weapon/armor assignment is now handled at runtime by EquipmentSystem when the player equips items, not at spawn time.

    In software.test.ts:
    - Replace any test setup that adds BurnedSoftware component to entities with EquipmentSlots + Children setup. Pattern: instead of world.addComponent(player, BurnedSoftware, { weapon: softwareId, armor: null }), use world.addComponent(player, EquipmentSlots, { weapon: weaponEntityId, armor: null }) + world.addComponent(weaponEntityId, Children, { entityIds: [softwareId] }).
    - Update imports accordingly: remove BurnedSoftware, add EquipmentSlots and Children.

    Delete src/shared/components/burned-software.ts entirely.

    Run grep -rn BurnedSoftware src/ to verify zero remaining references. If any stragglers found, fix them.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npx vitest run</automated>
  </verify>
  <acceptance_criteria>
    - src/shared/components/burned-software.ts does NOT exist (deleted)
    - engine-factory.ts contains playerOverrides entry 'equipmentSlots': { weapon: null, armor: null }
    - engine-factory.ts does NOT contain 'burnedSoftware' anywhere
    - software.test.ts does NOT import or reference BurnedSoftware
    - software.test.ts uses EquipmentSlots + Children for test entity setup
    - grep -rn BurnedSoftware src/ returns zero results
    - npx tsc --noEmit exits 0 (full TypeScript compilation clean)
    - npx vitest run exits 0 (full test suite green)
  </acceptance_criteria>
  <done>BurnedSoftware fully removed from codebase; player entity spawns with EquipmentSlots; all tests pass; TypeScript compiles cleanly</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| N/A | This plan is a migration sweep — no new trust boundaries introduced |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-05 | Information Disclosure | software-effects.ts traversal exposes child entities | accept | Same visibility model as before — entity components are only accessed server-side within system execution. No new surface. |
| T-19-SC | Tampering | npm installs | accept | No new packages installed in this phase. |
</threat_model>

<verification>
npx tsc --noEmit — zero compile errors (BurnedSoftware fully excised)
npx vitest run — full test suite green
grep -rn BurnedSoftware src/ — zero results
</verification>

<success_criteria>
- burned-software.ts file deleted
- Zero references to BurnedSoftware anywhere in src/
- software-effects.ts functions read from EquipmentSlots hierarchy
- run-ender.ts clears EquipmentSlots on death/extraction
- sync-bridge.ts populates mods from EquipmentSlots hierarchy
- engine-factory.ts initializes player with EquipmentSlots { weapon: null, armor: null }
- TypeScript compiles with zero errors
- Full vitest suite passes
</success_criteria>

<output>
Create `.planning/phases/19-combat-stat-pipeline-turn-costs/19-03-SUMMARY.md` when done
</output>
