---
phase: 19-combat-stat-pipeline-turn-costs
plan: 02
type: execute
wave: 2
depends_on: [19-01]
files_modified:
  - src/shared/components/intents.ts
  - src/shared/types.ts
  - src/shared/pipeline.ts
  - src/game/systems/equipment.ts
  - src/shared/__tests__/pipeline.test.ts
autonomous: true
requirements: [EQP-03]
must_haves:
  truths:
    - "D-04: Equipment actions are submitted as EquipIntent/UnequipIntent with extended slotType enum, routed through TurnManager.submitAction"
    - "D-05: When intent resolves at Phase.ACTION, equipment slot updates then turn cycle continues — enemies take their turns"
    - "Equipping a weapon via the action pipeline costs 1 full turn (enemies act after)"
    - "Unequipping a weapon via the action pipeline costs 1 full turn"
    - "Equipping armor via the action pipeline costs 1 full turn"
    - "Unequipping armor via the action pipeline costs 1 full turn"
    - "EquipmentSystem patches EquipmentSlots.weapon or .armor when processing weapon/armor EquipIntent"
  artifacts:
    - path: "src/shared/components/intents.ts"
      provides: "Extended EquipIntent/UnequipIntent with weapon/armor slotType values"
      contains: "'weapon', 'armor'"
    - path: "src/shared/types.ts"
      provides: "Extended EquipActionSchema slotType enum with weapon/armor"
      contains: "'weapon', 'armor'"
    - path: "src/game/systems/equipment.ts"
      provides: "weapon/armor case branches in handleEquip and handleUnequip"
      contains: "case 'weapon'"
    - path: "src/shared/__tests__/pipeline.test.ts"
      provides: "Integration tests for gear action round-trip"
      min_lines: 30
  key_links:
    - from: "src/shared/pipeline.ts"
      to: "src/shared/components/intents.ts"
      via: "import EquipIntent"
      pattern: "import.*EquipIntent"
    - from: "src/game/systems/equipment.ts"
      to: "src/shared/components/equipment-slots.ts"
      via: "import EquipmentSlots for patchComponent"
      pattern: "import.*EquipmentSlots"
---

<objective>
Wire equipment management actions (Equip/Uninstall weapon/armor) through the intent pipeline so they cost 1 full turn via TurnManager.

Purpose: Satisfies EQP-03 — every equipment action (Equip, Uninstall) increments the game turn counter. The player spends their action on gear management; enemies respond.

Output: Extended intent schemas, updated pipeline routing, updated EquipmentSystem handler, and passing integration tests.
</objective>

<execution_context>
@/Users/cmowery/dev/chadspace/nirmata-run-feature-phase-18/.claude/get-shit-done/workflows/execute-plan.md
@/Users/cmowery/dev/chadspace/nirmata-run-feature-phase-18/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-combat-stat-pipeline-turn-costs/19-PATTERNS.md
@.planning/phases/19-combat-stat-pipeline-turn-costs/19-RESEARCH.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend intent schemas and action types for weapon/armor slots</name>
  <files>src/shared/components/intents.ts, src/shared/types.ts, src/shared/pipeline.ts</files>
  <read_first>
    - src/shared/components/intents.ts (current EquipIntent at ~lines 68-74, UnequipIntent at ~lines 79-85 — see exact enum values)
    - src/shared/types.ts (EquipActionSchema at ~lines 26-31, UnequipActionSchema nearby, ActionIntentSchema discriminatedUnion at ~lines 104-122)
    - src/shared/pipeline.ts (processAction switch — case 'EQUIP' at ~lines 114-116, case 'UNEQUIP' nearby, imports at ~lines 10-15)
  </read_first>
  <behavior>
    - EquipIntent.slotType Zod enum accepts 'weapon' and 'armor' in addition to existing values
    - UnequipIntent.slotType Zod enum accepts 'weapon' and 'armor' in addition to existing values
    - EquipActionSchema.slotType Zod enum accepts 'weapon' and 'armor'
    - UnequipActionSchema.slotType Zod enum accepts 'weapon' and 'armor' (if it has slotType)
    - processAction case 'EQUIP' with slotType 'weapon' or 'armor' attaches EquipIntent correctly
  </behavior>
  <action>
    In intents.ts: extend the EquipIntent z.enum from ['firmware', 'software', 'augment'] to ['firmware', 'software', 'augment', 'weapon', 'armor']. Apply the same extension to UnequipIntent z.enum.

    In types.ts: extend the EquipActionSchema slotType z.enum from ['firmware', 'augment', 'software'] to ['firmware', 'augment', 'software', 'weapon', 'armor']. If UnequipActionSchema has a slotType field, extend it identically. The ActionIntentSchema discriminatedUnion does NOT need new entries — the existing EQUIP/UNEQUIP types now accept the broader enum.

    In pipeline.ts: verify that the existing case 'EQUIP' passes action.slotType through to EquipIntent without filtering — if it does, NO pipeline changes are needed (the extended enum handles routing). If there is any slotType validation/guard in the switch case, remove it so 'weapon'/'armor' pass through.

    Per D-04: Equipment actions use the existing EquipIntent/UnequipIntent with extended slotType enum. Per D-05: TurnManager.submitAction already deducts defaultActionCost (1000 energy) for all non-WAIT actions — no TurnManager changes needed.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - intents.ts EquipIntent schema slotType enum contains 'weapon' and 'armor'
    - intents.ts UnequipIntent schema slotType enum contains 'weapon' and 'armor'
    - types.ts EquipActionSchema slotType enum contains 'weapon' and 'armor'
    - npx tsc --noEmit exits 0
  </acceptance_criteria>
  <done>Intent and action schemas accept weapon/armor slot types; pipeline passes them through without filtering</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Update EquipmentSystem to handle weapon/armor slot types and write integration tests</name>
  <files>src/game/systems/equipment.ts, src/shared/__tests__/pipeline.test.ts</files>
  <read_first>
    - src/game/systems/equipment.ts (full file — handleEquip switch at ~lines 47-62, handleUnequip switch at ~lines 85-98, imports at ~lines 7-10, patchComponent pattern at line 77)
    - src/shared/__tests__/pipeline.test.ts (full file — existing test setup, entity creation, runActionPipeline usage, assertion patterns)
    - src/shared/components/equipment-slots.ts (EquipmentSlots export name and schema shape)
  </read_first>
  <behavior>
    - EquipmentSystem handleEquip with slotType 'weapon' patches EquipmentSlots.weapon to intent.itemEntityId
    - EquipmentSystem handleEquip with slotType 'armor' patches EquipmentSlots.armor to intent.itemEntityId
    - EquipmentSystem handleUnequip with slotType 'weapon' patches EquipmentSlots.weapon to null
    - EquipmentSystem handleUnequip with slotType 'armor' patches EquipmentSlots.armor to null
    - EQUIP action with slotType 'weapon' through runActionPipeline results in EquipmentSlots.weapon set
    - UNEQUIP action with slotType 'weapon' through runActionPipeline results in EquipmentSlots.weapon cleared
    - Turn cost is implicitly enforced (TurnManager.submitAction deducts energy on the client side)
  </behavior>
  <action>
    In equipment.ts imports: add EquipmentSlots to the import from '@shared/components'.

    In handleEquip switch (before the default: return): add case 'weapon' that reads EquipmentSlots via world.getComponent(entityId, EquipmentSlots), if null return early, then call w.patchComponent(entityId, EquipmentSlots, { weapon: intent.itemEntityId }), emit 'EQUIPMENT_CHANGED' event with { entityId, slotType: intent.slotType }, emit 'MESSAGE_EMITTED' with { text: 'Weapon equipped', type: 'info' }, log via logger.info, then return early (do not fall through to the generic equipped[] array logic). Add identical case 'armor' that patches { armor: intent.itemEntityId } with message 'Armor equipped'.

    In handleUnequip switch (before default: return): add case 'weapon' that reads EquipmentSlots, patches { weapon: null }, emits 'EQUIPMENT_CHANGED'. Add case 'armor' that patches { armor: null }.

    In pipeline.test.ts: add a new describe block 'Equipment gear actions (weapon/armor)'. Setup: create world with player entity having EquipmentSlots component { weapon: null, armor: null }. Tests: (1) EQUIP action with slotType 'weapon' and itemEntityId 99 results in EquipmentSlots.weapon === 99 after runActionPipeline; (2) UNEQUIP action with slotType 'weapon' results in EquipmentSlots.weapon === null; (3) same for armor variant. Follow existing test structure (beforeEach, describe, it).
  </action>
  <verify>
    <automated>npx vitest run src/shared/__tests__/pipeline.test.ts src/game/systems/equipment.test.ts 2>/dev/null; npx vitest run src/shared/__tests__/pipeline.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - equipment.ts contains case 'weapon': with w.patchComponent(entityId, EquipmentSlots, { weapon: intent.itemEntityId })
    - equipment.ts contains case 'armor': with w.patchComponent(entityId, EquipmentSlots, { armor: intent.itemEntityId })
    - equipment.ts handleUnequip contains case 'weapon' that patches { weapon: null }
    - equipment.ts handleUnequip contains case 'armor' that patches { armor: null }
    - pipeline.test.ts contains test for EQUIP action with slotType 'weapon' resulting in EquipmentSlots.weapon set
    - pipeline.test.ts contains test for UNEQUIP action with slotType 'weapon' clearing the slot
    - npx vitest run src/shared/__tests__/pipeline.test.ts exits 0
  </acceptance_criteria>
  <done>EquipmentSystem handles weapon/armor equip/unequip; integration tests verify round-trip through pipeline; turn cost is implicitly enforced by TurnManager.submitAction on client side</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client -> Server API | EQUIP/UNEQUIP actions with slotType validated by Zod ActionIntentSchema |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-03 | Tampering | EQUIP action with invalid itemEntityId | mitigate | EquipmentSystem reads EquipmentSlots from entityId — if component missing, returns early (no-op). Server reconciles authoritative state if client diverges. |
| T-19-04 | Spoofing | Forged EQUIP action referencing another player's entity | accept | Server validates session ownership of the action entity. Existing pattern for all actions. |
| T-19-SC | Tampering | npm installs | accept | No new packages installed in this phase. |
</threat_model>

<verification>
npx vitest run src/shared/__tests__/pipeline.test.ts — all pipeline tests pass including new gear action tests
npx vitest run src/game/systems/equipment.test.ts — existing equipment tests still pass (if file exists)
npx tsc --noEmit — no compile errors
</verification>

<success_criteria>
- EquipIntent/UnequipIntent schemas accept 'weapon' and 'armor' slotType values
- EquipActionSchema in types.ts accepts 'weapon' and 'armor'
- Pipeline routes EQUIP/UNEQUIP with weapon/armor slotType without filtering
- EquipmentSystem patches EquipmentSlots for weapon/armor slot types
- Integration tests verify full action round-trip (action -> intent -> system -> state change)
- Turn cost is inherent — TurnManager.submitAction deducts defaultActionCost for all actions
</success_criteria>

<output>
Create `.planning/phases/19-combat-stat-pipeline-turn-costs/19-02-SUMMARY.md` when done
</output>
