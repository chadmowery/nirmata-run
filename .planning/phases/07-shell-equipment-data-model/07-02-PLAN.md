---
wave: 2
depends_on: ["07-01-PLAN.md"]
files_modified:
  - src/shared/events/types.ts
  - src/shared/types.ts
  - src/shared/pipeline.ts
  - src/game/systems/equipment.ts
  - src/game/engine-factory.ts
  - src/game/setup.ts
  - src/game/systems/__tests__/equipment.test.ts
  - src/tests/integration/shell-to-player.test.ts
autonomous: true
requirements: ["SHELL-02", "SHELL-03"]
---

# Phase 7 - Plan 02: Equipment Slot System & Shell Selection

<objective>
To implement equipment slot validation against port configs via the action pipeline, and run initial Shell selection to stamp base stats to ephemeral player entities.
</objective>

<task type="auto">
  <read_first>
    - src/shared/events/types.ts
    - src/shared/types.ts
  </read_first>
  <action>
    In `src/shared/events/types.ts`, add to `GameplayEvents`:
    - `SHELL_STATS_CHANGED: { entityId: number, shellId: string }`
    - `EQUIPMENT_CHANGED: { entityId: number, slotType: string }`
    - `SHELL_SELECTED: { shellId: string }`
    In `src/shared/types.ts`, add to `ActionIntentSchema`:
    - `EquipActionSchema = z.object({ type: z.literal('EQUIP'), shellId: z.string(), slotType: z.enum(['firmware', 'augment', 'software']), itemEntityId: z.number() })`
    - `UnequipActionSchema = z.object({ type: z.literal('UNEQUIP'), slotType: z.enum(['firmware', 'augment', 'software']), slotIndex: z.number() })`
    - `SelectShellActionSchema = z.object({ type: z.literal('SELECT_SHELL'), shellId: z.string() })`
    Add them to the `ActionIntentSchema` discriminated union.
  </action>
  <acceptance_criteria>
    - `grep -q "SHELL_STATS_CHANGED" src/shared/events/types.ts` exits 0
    - `grep -q "EQUIP" src/shared/types.ts` exits 0
  </acceptance_criteria>
</task>

<task type="tdd">
  <read_first>
    - src/shared/pipeline.ts
    - src/game/types.ts
  </read_first>
  <action>
    Create `src/game/systems/equipment.ts` with `handleEquip` and `handleUnequip` logic.
    Business rule: check `world.getComponent(entityId, PortConfig)` to validate max slot count before appending `itemEntityId` to the `<type>Slots` component `equipped` array. If full, emit `MESSAGE_EMITTED` error type and return.
    In `src/shared/pipeline.ts`, add a switch-case for `EQUIP` and `UNEQUIP` actions invoking helper methods. Export `handleEquip` and `handleUnequip` from `equipment.ts` and use them in `pipeline.ts`.
    Create `src/game/systems/__tests__/equipment.test.ts` to test slot limit enforcement.
  </action>
  <acceptance_criteria>
    - `grep -q "handleEquip" src/shared/pipeline.ts` exits 0
    - `npx vitest run src/game/systems/__tests__/equipment.test.ts` exits 0
  </acceptance_criteria>
</task>

<task type="tdd">
  <read_first>
    - src/game/engine-factory.ts
    - src/game/setup.ts
  </read_first>
  <action>
    Update `src/game/engine-factory.ts`'s `EngineInitConfig` to optionally take `shellRecord?: any`.
    During `placeEntities` for the player, if `shellRecord` is provided, generate `runtimeOverrides` for `health` (max, current based on currentStats.maxHealth), `defense` (armor), and `energy` (speed). Also stamp `PortConfig`, `Shell`, `FirmwareSlots` (empty array), `AugmentSlots`, and `SoftwareSlots` onto the player.
    Update `src/game/setup.ts` `GameConfig` to optionally take `shellId?: string`. At run start, fetch from Global `SessionManager`/`ShellRegistry` (stubbed for Phase 7) and pass as `shellRecord` to engine factory.
    Create a test `src/tests/integration/shell-to-player.test.ts` that initializes an engine with `shellRecord: { id: "test", archetypeId: "test", level: 1, currentStats: { speed: 120, maxHealth: 15, armor: 1 }, portConfig: { maxFirmware: 2, maxAugment: 1, maxSoftware: 2 } }` and verifies the spawned player has Health max=15 and Defense armor=1.
  </action>
  <acceptance_criteria>
    - `grep -q "shellRecord" src/game/engine-factory.ts` exits 0
    - `npx vitest run src/tests/integration/shell-to-player.test.ts` exits 0
  </acceptance_criteria>
</task>
