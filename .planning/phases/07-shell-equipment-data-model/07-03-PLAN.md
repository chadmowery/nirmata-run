---
wave: 2
depends_on: ["07-01-PLAN.md"]
files_modified:
  - src/shared/types.ts
  - src/shared/pipeline.ts
  - src/game/systems/shell-stats.ts
  - src/game/shells/shell-registry.ts
  - src/tests/integration/death-equipment-clear.test.ts
  - src/tests/integration/shell-upgrade.test.ts
autonomous: true
requirements: ["SHELL-04", "SHELL-05", "SHELL-06"]
---

# Phase 7 - Plan 03: Shell Persistence & Upgrades

<objective>
To implement death clearing logic for equipment while preserving the Shell, handle upgrade transactions, and outline shell availability.
</objective>

<task type="tdd">
  <read_first>
    - src/shared/pipeline.ts
  </read_first>
  <action>
    In `src/shared/pipeline.ts` `setupInternalHandlers`, add logic to listen to the `ENTITY_DIED` event. If the dying entity has `FirmwareSlots`, `AugmentSlots`, or `SoftwareSlots`, reset their `equipped` arrays to empty explicitly before the entity gets fully cleaned up from the grid (or test state). 
    Wait, `destroyEntity` removes components entirely. But we want to simulate that equipment is lost on death while the `ShellRecord` (which lives in `ShellRegistry`, not ECS) survives.
    Create a test in `src/tests/integration/death-equipment-clear.test.ts` to simulate death: give player components, trigger `ENTITY_DIED`, and verify that the `ShellRecord` in `ShellRegistry` remains intact without being affected by the ECS `destroyEntity`. In `setupInternalHandlers`, ensure `ENTITY_DIED` resets slots if we want to retain the entity for a game over screen before full cleanup.
  </action>
  <acceptance_criteria>
    - `npx vitest run src/tests/integration/death-equipment-clear.test.ts` exits 0
  </acceptance_criteria>
</task>

<task type="tdd">
  <read_first>
    - src/shared/types.ts
    - src/shared/pipeline.ts
    - src/game/shells/shell-registry.ts
  </read_first>
  <action>
    Add `UpgradeShellActionSchema = z.object({ type: z.literal('UPGRADE_SHELL'), shellId: z.string() })` to `src/shared/types.ts` `ActionIntentSchema` if not present.
    Create `src/game/systems/shell-stats.ts`. This file will contain `propagateShellStats(world, entityId)` that reads `ShellComponent` and updates the entity's `Health` (max/current adjust), `Energy` (speed), and `Defense` (armor) according to `ShellComponent` values.
    Also have it listen to `SHELL_STATS_CHANGED` on the `eventBus` to call `propagateShellStats`.
    Implement `handleUpgradeShell(registry, action)` to mutate the `ShellRecord` level up and apply stats, then emit `SHELL_STATS_CHANGED` (if player is instantiated).
    Create `src/tests/integration/shell-upgrade.test.ts` to test taking an entity, upgrading the Shell, and verifying its Max Health updates correctly via the stats propagation system.
  </action>
  <acceptance_criteria>
    - `grep -q "propagateShellStats" src/game/systems/shell-stats.ts` exits 0
    - `npx vitest run src/tests/integration/shell-upgrade.test.ts` exits 0
  </acceptance_criteria>
</task>

<task type="auto">
  <read_first>
    - src/game/shells/shell-registry.ts
  </read_first>
  <action>
    In `src/game/shells/shell-registry.ts`, add a function `getAvailableShells(weekSeed?: string): ShellRecord[]` to specify availability for SHELL-06. If a `weekSeed` is provided, deterministically return a subset (e.g., 2) of registered shells. If not, return all.
  </action>
  <acceptance_criteria>
    - `grep -q "getAvailableShells" src/game/shells/shell-registry.ts` exits 0
  </acceptance_criteria>
</task>
