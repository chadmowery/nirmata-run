---
phase: 06-ui-integration
plan: 02
type: execute
wave: 2
depends_on: ["06-01-PLAN.md"]
files_modified:
  - src/app/layout.tsx
  - src/app/page.tsx
  - src/app/globals.css
  - src/components/ui/PlayerHUD.tsx
  - src/components/ui/MessageLog.tsx
  - src/components/ui/NearbyEntities.tsx
  - src/components/ui/styles.module.css
autonomous: true
requirements:
  - UI-02
  - UI-03
  - UI-04
---

<objective>
Build the core game UI components in React and connect them to the store.

Purpose: To create a functional and aesthetically pleasing "Classic Terminal" UI overlay for the game, allowing the player to see their stats, read the combat log, and identify nearby threats.
Output: A React-based HUD mounted over the PixiJS canvas, reactively updated by the Zustand store.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-ui-integration/06-RESEARCH.md
@.planning/phases/06-ui-integration/06-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold Next.js App Structure</name>
  <files>src/app/layout.tsx, src/app/page.tsx, src/app/globals.css</files>
  <behavior>
    - Next.js layout and page are configured for a full-screen game experience
    - PixiJS canvas is mounted in a stable container
    - UI overlay is positioned correctly (z-index) over the canvas
  </behavior>
  <action>
    1. Create `src/app/layout.tsx` with basic HTML structure and viewport settings.
    2. Create `src/app/page.tsx`:
       - Use a `useEffect` to bootstrap the game engine (via `setup.ts`).
       - Mount the canvas element.
       - Render the `HUDOverlay` component (to be created).
    3. Create `src/app/globals.css`:
       - Set body/html to `overflow: hidden`, `margin: 0`, `background: black`.
       - Define monospace font families.
  </action>
  <verify>
    <manual>Run `npm run dev` and confirm the PixiJS canvas renders full-screen with black background.</manual>
  </verify>
  <done>Next.js scaffolded with PixiJS canvas mounted</done>
</task>

<task type="auto">
  <name>Task 2: Implement Classic Terminal Styling</name>
  <files>src/components/ui/styles.module.css</files>
  <behavior>
    - CSS Modules provide a retro terminal aesthetic
    - Borders use ASCII-style patterns or high-contrast lines
    - Colors are limited to a retro palette (e.g., Amber/Green on Black)
  </behavior>
  <action>
    1. Define CSS classes for `.container`, `.hud`, `.messageLog`, `.entityList`.
    2. Implement "Scanline" or "CRT" effects via CSS overlays (optional).
    3. Ensure all interactive elements have clear hover/active states in a terminal style.
  </action>
  <verify>
    <manual>Confirm UI elements have the intended retro look when rendered.</manual>
  </verify>
  <done>Terminal aesthetic defined in CSS Modules</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Implement PlayerHUD Component</name>
  <files>src/components/ui/PlayerHUD.tsx</files>
  <behavior>
    - Displays HP/Max HP and XP/Level bars
    - Updates reactively when `player` stats in `gameStore` change
    - Shows icons for active buffs/debuffs
  </behavior>
  <action>
    1. Create `PlayerHUD` using `useStore(gameStore)` with granular selectors.
    2. Implement visual bars (e.g., `[#####---]`) or styled progress elements.
    3. Map active statuses to Lucide React icons or text labels.
  </action>
  <verify>
    <automated>npx vitest src/components/ui/__tests__/PlayerHUD.test.tsx</automated>
  </verify>
  <done>Player HUD component implemented and reactive</done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Implement MessageLog Component</name>
  <files>src/components/ui/MessageLog.tsx</files>
  <behavior>
    - Displays scrollable list of game messages
    - Messages have different styles based on type (combat: red/yellow, info: white, error: bright red)
    - Auto-scrolls to the bottom on new messages
  </behavior>
  <action>
    1. Create `MessageLog` using `useStore(gameStore, s => s.messages)`.
    2. Implement `messages.map` with appropriate coloring.
    3. Handle the "collapsed" count display (e.g., "The orc hits you (x3)").
    4. Use a `useRef` and `useEffect` to manage scroll position.
  </action>
  <verify>
    <automated>npx vitest src/components/ui/__tests__/MessageLog.test.tsx</automated>
  </verify>
  <done>Message log component implemented and correctly stylized</done>
</task>

<task type="auto" tdd="true">
  <name>Task 5: Implement NearbyEntities Component</name>
  <files>src/components/ui/NearbyEntities.tsx</files>
  <behavior>
    - Displays a list of entities currently in the player's FOV
    - Interactive: hover/click could potentially highlight the entity in PixiJS (via store metadata)
  </behavior>
  <action>
    1. Update `gameStore` to include `visibleEntities`.
    2. Update `sync-bridge.ts` to push FOV entities to the store on every turn/move.
    3. Implement `NearbyEntities` list with health bars for visible enemies.
  </action>
  <verify>
    <automated>npx vitest src/components/ui/__tests__/NearbyEntities.test.tsx</automated>
  </verify>
  <done>Nearby entities list implemented and reactive to FOV changes</done>
</task>

</tasks>

<verification>
1. `npx vitest src/components/ui/__tests__/*.test.tsx` — All React component tests pass.
2. `npm run dev` — Visual check of the UI layout over the game canvas.
3. Take damage in-game (simulated or real) and verify the HP bar in the HUD updates.
</verification>

<success_criteria>
- HUD shows correct player health and XP.
- Message log displays combat events and collapses repeats.
- Nearby entities list correctly reflects what the player can see in FOV.
- UI follows "Classic Terminal" aesthetic consistently.
- No React re-render cascades (verified via React DevTools or performance profiling).
</success_criteria>

<output>
After completion, create `.planning/phases/06-ui-integration/06-02-SUMMARY.md`
</output>
