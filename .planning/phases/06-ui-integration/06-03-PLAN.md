---
phase: 06-ui-integration
plan: 03
type: execute
wave: 3
depends_on: ["06-02-PLAN.md"]
files_modified:
  - src/app/page.tsx
  - src/components/ui/MainMenu.tsx
  - src/components/ui/GameOver.tsx
  - src/game/input/input-bridge.ts
  - src/tests/integration/tech-demo.test.ts
autonomous: true
requirements:
  - UI-05
  - UI-06
  - SRV-06
---

<objective>
Finalize the game flow with menus and ensure the end-to-end tech demo is fully integrated.

Purpose: To provide a complete game lifecycle (Start -> Play -> Death -> Restart) and bridge the UI interactions back into the engine's action system.
Output: A polished, playable tech demo with functional menus and end-to-end integration.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-ui-integration/06-RESEARCH.md
@.planning/phases/06-ui-integration/06-CONTEXT.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement MainMenu Screen</name>
  <files>src/components/ui/MainMenu.tsx</files>
  <behavior>
    - Screen appears when `gameStatus` is `'menu'`
    - "Start Game" button transitions store state to `'loading'` or `'playing'`
    - Triggers the engine bootstrap process
  </behavior>
  <action>
    1. Create `MainMenu` component with a high-impact "Title" and "Start Game" button.
    2. Button click calls `gameStore.getState().setGameStatus('playing')`.
    3. Ensure `src/app/page.tsx` reacts to this status change to initialize/start the engine.
  </action>
  <verify>
    <automated>npx vitest src/components/ui/__tests__/MainMenu.test.tsx</automated>
  </verify>
  <done>Main menu screen implemented and functional</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement GameOver Screen</name>
  <files>src/components/ui/GameOver.tsx</files>
  <behavior>
    - Screen appears when `gameStatus` is `'gameOver'` (triggered by `ENTITY_DIED` event for player)
    - Displays a summary of the run: turns survived, enemies killed, levels cleared
    - "Restart" button reloads the page or re-initializes the engine
  </behavior>
  <action>
    1. Update `sync-bridge.ts` to detect player death and call `setGameStatus('gameOver')`.
    2. Create `GameOver` component with run statistics.
    3. "Restart" button should cleanly destroy the previous game instance and start a new one.
  </action>
  <verify>
    <automated>npx vitest src/components/ui/__tests__/GameOver.test.tsx</automated>
  </verify>
  <done>Game over screen with run summary implemented</done>
</task>

<task type="auto">
  <name>Task 3: Implement Input Bridge</name>
  <files>src/game/input/input-bridge.ts</files>
  <behavior>
    - UI buttons (like "Wait") correctly dispatch semantic actions to the `InputSystem`
    - Actions dispatched from UI are treated identically to keyboard inputs (validated by server)
  </behavior>
  <action>
    1. Create `dispatchUIAction(action: GameAction)` function.
    2. Hook this function into the existing `InputSystem` action pipeline.
    3. Add a "Wait" button to the HUD that calls this bridge.
  </action>
  <verify>
    <manual>Click "Wait" button in the HUD and confirm a turn passes and player energy is consumed.</manual>
  </verify>
  <done>UI-to-Engine input bridge functional</done>
</task>

<task type="auto">
  <name>Task 4: Finalize Game Layout & Focus Management</name>
  <files>src/app/page.tsx</files>
  <behavior>
    - Page handles transitions between Menu -> Loading -> Playing -> Game Over
    - Keyboard focus is managed so the game remains playable after UI clicks
    - Idempotent cleanup of PixiJS and EventBus listeners
  </behavior>
  <action>
    1. Refactor `src/app/page.tsx` to switch between `MainMenu`, `HUDOverlay`, and `GameOver` based on store status.
    2. Ensure `useEffect` cleanup properly calls `engine.destroy()`.
    3. Add a "focus trap" or global key listener to ensure the canvas doesn't lose focus during play.
  </action>
  <verify>
    <manual>Verify full game loop: Menu -> Play (move/fight) -> Die -> Game Over -> Restart.</manual>
  </verify>
  <done>End-to-end game flow finalized in Next.js page</done>
</task>

<task type="auto" tdd="true">
  <name>Task 5: End-to-End Integration Test</name>
  <files>src/tests/integration/tech-demo.test.ts</files>
  <behavior>
    - Test mocks a full game session: starts game, takes several actions, kills an enemy, gets killed
    - Asserts that UI store state is correct at each step
    - Verifies that server validation (Phase 5) is still functioning within the UI context
  </behavior>
  <action>
    1. Create an integration test that uses the real `EventBus`, `World`, and `gameStore`.
    2. Simulate a series of events.
    3. Assert `gameStore` snapshots match expected outcomes.
  </action>
  <verify>
    <automated>npx vitest src/tests/integration/tech-demo.test.ts</automated>
  </verify>
  <done>End-to-end integration tests confirm tech demo stability</done>
</task>

</tasks>

<verification>
1. `npx vitest src/tests/integration/tech-demo.test.ts` — Integration test passes.
2. `npm run build` — Verify the Next.js project builds without errors.
3. Manual Playtest:
   - Spawn in generated dungeon.
   - Explore (FOV updates).
   - Fight an enemy (Log shows hits/damage).
   - Pick up an item (HUD reflects stats).
   - Die (Game Over screen shows).
   - Restart (New world generated).
</verification>

<success_criteria>
- Game flow is seamless from Main Menu to Game Over.
- UI interactions (buttons) correctly influence the game state.
- Integration tests cover the full "Happy Path" and "Death" scenarios.
- Tech demo is fully integrated with Phase 5 server authority (mocked or real API).
</success_criteria>

<output>
After completion, create `.planning/phases/06-ui-integration/06-03-SUMMARY.md`
</output>
