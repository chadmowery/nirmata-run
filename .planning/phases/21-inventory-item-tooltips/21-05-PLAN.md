---
phase: 21-inventory-item-tooltips
plan: 5
type: execute
wave: 4
depends_on: [2, 3, 4]
files_modified:
  - src/components/ui/inventory/BackpackGrid.tsx
  - src/components/ui/inventory/EquipmentPanel.tsx
  - src/components/ui/inventory/InventoryOverlay.tsx
autonomous: true
requirements: [UI-02]
must_haves:
  truths:
    - User can drag and drop items between slots
    - Invalid drop targets prevent highlighting
    - Click-fallback equips/unequips items with a toast on failure
  artifacts:
    - path: src/components/ui/inventory/BackpackGrid.tsx
      provides: Drag and drop event handlers
  key_links:
    - from: src/components/ui/inventory/BackpackGrid.tsx
      to: src/game/intent/inventory-intent.ts
      via: API intent dispatch
---

<objective>
Implement the interactive drag-and-drop mechanics and click fallbacks for the inventory UI.

Purpose: Covers D-04 (Manual Drag & Drop and click fallback), D-10 (Invalid targets prevent highlight), and D-11 (Brief Toast for invalid click fallback).
Output: Fully interactive drag-and-drop inventory slots.
</objective>

<execution_context>
@/Users/chadmowery/personal/dev/dungeon-runner/nimrata-run/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<tasks>

<task type="auto">
  <name>Task 1: Implement Drag & Drop Interaction (D-04, D-10)</name>
  <files>src/components/ui/inventory/BackpackGrid.tsx, src/components/ui/inventory/EquipmentPanel.tsx</files>
  <action>
    Modify UI components to support native HTML5 drag-and-drop:
    - Make items `draggable`.
    - Implement `onDragStart`, setting data transfer for the item ID and source slot.
    - Implement `onDragOver` and `onDrop` on slots.
    - Implement D-10: During `onDragOver`, check if the dragged item is valid for the target slot (e.g., armor into a weapon slot is invalid). If invalid, do not highlight the slot and `preventDefault()` conditionally to disallow drop.
    - On successful `onDrop`, dispatch the `INVENTORY_SWAP` intent (created in Plan 4) to the backend.
  </action>
  <verify>
    <automated>npx eslint src/components/ui/inventory --ext .tsx --no-error-on-unmatched-pattern</automated>
  </verify>
  <done>
    - Drag-and-drop allows swapping items.
    - Invalid drops prevent highlight (D-10).
  </done>
</task>

<task type="auto">
  <name>Task 2: Click Fallback & Invalid Toast (D-04, D-11)</name>
  <files>src/components/ui/inventory/BackpackGrid.tsx, src/components/ui/inventory/EquipmentPanel.tsx, src/components/ui/inventory/InventoryOverlay.tsx</files>
  <action>
    Modify UI components to support a click fallback:
    - Implement D-04: Add double-click or right-click (`onContextMenu`) handlers to auto-equip an item from the backpack, or unequip it to the backpack.
    - Implement D-11: If the fallback action is invalid (e.g., trying to equip a weapon when the slot is full and backpack is full, or wrong slot type), render a brief visual Toast or inline text notification (e.g., "Invalid Target") over the inventory overlay.
  </action>
  <verify>
    <automated>npx eslint src/components/ui/inventory --ext .tsx --no-error-on-unmatched-pattern</automated>
  </verify>
  <done>
    - Double-click/right-click performs quick equip/unequip.
    - Invalid fallback actions trigger a Toast (D-11).
  </done>
</task>

</tasks>

<verification>
npx tsc --noEmit
</verification>

<success_criteria>
The inventory is fully interactive with both drag-and-drop and click fallbacks, correctly validating targets and providing feedback.
</success_criteria>

<output>
After completion, create `.planning/phases/21-inventory-item-tooltips/21-05-SUMMARY.md`
</output>