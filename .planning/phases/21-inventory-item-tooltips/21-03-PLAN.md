---
phase: 21-inventory-item-tooltips
plan: 3
type: execute
wave: 3
depends_on: [2]
files_modified:
  - src/components/ui/inventory/InRunItemTooltip.tsx
  - src/components/ui/inventory/BackpackGrid.tsx
  - src/components/ui/inventory/EquipmentPanel.tsx
  - src/components/ui/inventory/InRunItemTooltip.test.tsx
autonomous: true
requirements: [UI-03]
must_haves:
  truths:
    - User can hover over an item to see its tooltip
    - Tooltip shows stats and software modifiers
    - Holding Alt expands tooltip to show deep stats
  artifacts:
    - path: src/components/ui/inventory/InRunItemTooltip.tsx
      provides: Tooltip component reading ECS data
  key_links:
    - from: src/components/ui/inventory/BackpackGrid.tsx
      to: src/components/ui/inventory/InRunItemTooltip.tsx
      via: render on hover
---

<objective>
Implement the custom InRunItemTooltip for inventory items.

Purpose: To display detailed stats and software modifiers when hovering over items, fulfilling D-06 and D-07.
Output: The `InRunItemTooltip` component integrated into the inventory slots.
</objective>

<execution_context>
@/Users/chadmowery/personal/dev/dungeon-runner/nimrata-run/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create Tooltip Component</name>
  <files>src/components/ui/inventory/InRunItemTooltip.tsx, src/components/ui/inventory/InRunItemTooltip.test.tsx</files>
  <behavior>
    - Test 1: Tooltip renders basic stats by default
    - Test 2: Tooltip renders expanded deep stats when Alt is held
  </behavior>
  <action>
    Create `src/components/ui/inventory/InRunItemTooltip.tsx`:
    - Takes `entityId: number` as a prop.
    - Reads `EquipmentDef` and `SoftwareDef` (if applicable) from `window.gameContext.world` for the given ID.
    - If it's equipment, queries `SoftwareSlots` to get installed software IDs, then queries their `SoftwareDef` to list modifiers.
    - Renders stats according to the Vibrant Decay aesthetic (pure CSS, no external tooltip library).
    - Implement D-06: Tiered tooltips. Listen for the 'Alt' key being held down via an event listener or custom hook. Show basic stats by default; when Alt is held, expand to show deeper stats and software modifiers.
  </action>
  <verify>
    <automated>npx vitest run src/components/ui/inventory/InRunItemTooltip.test.tsx</automated>
  </verify>
  <done>
    - `src/components/ui/inventory/InRunItemTooltip.tsx` exists and implements D-06 Alt key expansion
    - Reads `window.gameContext.world`
  </done>
</task>

<task type="auto">
  <name>Task 2: Wire Tooltips to Grid and Equipment Panels</name>
  <files>src/components/ui/inventory/BackpackGrid.tsx, src/components/ui/inventory/EquipmentPanel.tsx</files>
  <action>
    Modify `src/components/ui/inventory/BackpackGrid.tsx` and `src/components/ui/inventory/EquipmentPanel.tsx`:
    - Add hover state management (e.g., `hoveredEntityId` in component state).
    - On `onMouseEnter` of a slot with an item, set `hoveredEntityId`.
    - On `onMouseLeave`, clear it.
    - Conditionally render `<InRunItemTooltip entityId={hoveredEntityId} />` anchored to the item (D-07).
  </action>
  <verify>
    <automated>npx eslint src/components/ui/inventory/BackpackGrid.tsx src/components/ui/inventory/EquipmentPanel.tsx</automated>
  </verify>
  <done>
    - `src/components/ui/inventory/BackpackGrid.tsx` imports `InRunItemTooltip`
    - `src/components/ui/inventory/EquipmentPanel.tsx` imports `InRunItemTooltip`
  </done>
</task>

</tasks>

<verification>
npx tsc --noEmit
</verification>

<success_criteria>
Hovering over any item in the inventory overlay displays the custom tooltip with accurate ECS component data, expandable via the Alt key.
</success_criteria>

<output>
After completion, create `.planning/phases/21-inventory-item-tooltips/21-03-SUMMARY.md`
</output>