---
phase: 21-inventory-item-tooltips
plan: 4
type: execute
wave: 2
depends_on: [1]
files_modified:
  - src/game/intent/inventory-intent.ts
  - src/game/intent/inventory-intent.test.ts
  - src/game/systems/ItemPickupSystem.ts
  - src/game/systems/ItemPickupSystem.test.ts
  - src/app/api/action/route.ts
autonomous: true
requirements: [UI-02, UI-03]
must_haves:
  truths:
    - Item swapping costs 1 turn
    - Picked up loot auto-fills the first empty inventory slot
  artifacts:
    - path: src/game/intent/inventory-intent.ts
      provides: backend logic for inventory swapping
  key_links:
    - from: src/app/api/action/route.ts
      to: src/game/intent/inventory-intent.ts
      via: intent handling
---

<objective>
Implement the backend mechanics for inventory management, ensuring drag-and-drop actions are properly validated and executed as turn-based actions.

Purpose: Covers D-05 (Swapping items costs 1 turn) and D-09 (Loot automatically fills first empty cell).
Output: Handlers for INVENTORY_SWAP intent and updated ItemPickupSystem.
</objective>

<execution_context>
@/Users/chadmowery/personal/dev/dungeon-runner/nimrata-run/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement Inventory Swap Intent (D-05)</name>
  <files>src/game/intent/inventory-intent.ts, src/game/intent/inventory-intent.test.ts, src/app/api/action/route.ts</files>
  <behavior>
    - Test 1: Swap intent correctly moves item between slots
    - Test 2: Swap intent deducts 1 turn energy from the actor
  </behavior>
  <action>
    Create `src/game/intent/inventory-intent.ts`:
    - Define an `INVENTORY_SWAP` intent handler.
    - It should accept a source (e.g., gridIndex or equipment slot) and destination.
    - Implement D-05: Upon successful swap or equip action, deduct 1 turn's worth of energy/speed from the actor using the TurnManager/Energy components.
    
    Modify `src/app/api/action/route.ts`:
    - Register the `INVENTORY_SWAP` intent with the API route.
  </action>
  <verify>
    <automated>npx vitest run src/game/intent/inventory-intent.test.ts</automated>
  </verify>
  <done>
    - `INVENTORY_SWAP` intent exists and charges 1 turn per D-05.
    - API route accepts the new intent.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Auto-fill Loot Logic (D-09)</name>
  <files>src/game/systems/ItemPickupSystem.ts, src/game/systems/ItemPickupSystem.test.ts</files>
  <behavior>
    - Test 1: Item pickup assigns to the first empty gridIndex
    - Test 2: Item pickup fails if inventory is full
  </behavior>
  <action>
    Modify `src/game/systems/ItemPickupSystem.ts` (or the relevant pickup logic):
    - Implement D-09: When picking up ground loot, automatically find the first empty `gridIndex` (from 0 to maxSlots-1) in the actor's `RunInventory` component.
    - Assign the `gridIndex` to the picked up item.
  </action>
  <verify>
    <automated>npx vitest run src/game/systems/ItemPickupSystem.test.ts</automated>
  </verify>
  <done>
    - Loot is automatically assigned an empty gridIndex upon pickup.
  </done>
</task>

</tasks>

<verification>
npx tsc --noEmit
</verification>

<success_criteria>
Backend accurately processes inventory swaps costing 1 turn and auto-fills new loot into grid spaces.
</success_criteria>

<output>
After completion, create `.planning/phases/21-inventory-item-tooltips/21-04-SUMMARY.md`
</output>