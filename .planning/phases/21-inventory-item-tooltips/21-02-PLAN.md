---
phase: 21-inventory-item-tooltips
plan: 2
type: execute
wave: 2
depends_on: [1]
files_modified:
  - src/components/ui/inventory/InventoryOverlay.module.css
  - src/components/ui/inventory/EquipmentPanel.tsx
  - src/components/ui/inventory/BackpackGrid.tsx
  - src/components/ui/inventory/InventoryOverlay.tsx
  - src/components/ui/HUDOverlay.tsx
  - src/components/ui/inventory/EquipmentPanel.test.tsx
  - src/components/ui/inventory/BackpackGrid.test.tsx
  - src/components/ui/inventory/InventoryOverlay.test.tsx
autonomous: true
requirements: [UI-01]
must_haves:
  truths:
    - User can see equipped items in the Equipment Panel
    - User can see backpack items in a grid layout
    - Inventory overlay dims the background
  artifacts:
    - path: src/components/ui/inventory/InventoryOverlay.tsx
      provides: Split layout container
    - path: src/components/ui/inventory/EquipmentPanel.tsx
      provides: Display of Shell equipment
    - path: src/components/ui/inventory/BackpackGrid.tsx
      provides: Display of run inventory
  key_links:
    - from: src/components/ui/HUDOverlay.tsx
      to: src/components/ui/inventory/InventoryOverlay.tsx
      via: conditional render based on inventoryVisible
---

<objective>
Implement the split layout UI components for the Inventory Overlay.

Purpose: To display the player's equipped items and backpack contents in a "Vibrant Decay" themed overlay, fulfilling D-01, D-02, D-03, and D-08.
Output: The `InventoryOverlay` component containing `EquipmentPanel` and `BackpackGrid`, integrated into the main HUD.
</objective>

<execution_context>
@/Users/chadmowery/personal/dev/dungeon-runner/nimrata-run/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<tasks>

<task type="auto">
  <name>Task 1: Create Overlay Styles</name>
  <files>src/components/ui/inventory/InventoryOverlay.module.css</files>
  <action>
    Create `src/components/ui/inventory/InventoryOverlay.module.css`:
    Define styles conforming to the UI-SPEC (Vibrant Decay):
    - `.overlayContainer`: fixed, inset-0, z-50, flex, items-center, justify-center, bg-black/80 (dims background).
    - `.splitLayout`: flex, gap-xl (32px), p-2xl (48px), max-w-6xl, w-full.
    - `.panel`: flex-1, bg-[rgba(0,20,0,0.85)], border border-[#00ff41], p-lg (24px).
    - `.gridContainer`: display grid, grid-template-columns repeat(5, 1fr), gap-sm (8px).
    - `.slot`: w-16 h-16 border border-gray-700 flex items-center justify-center.
    - `.slotFilled`: border-[#00F0FF] text-[#00F0FF].
    - `.softwareIndicator`: styling for nested software slots.
    Use pure CSS matching the properties in the design contract.
  </action>
  <verify>
    <automated>test -f src/components/ui/inventory/InventoryOverlay.module.css && grep -q ".overlayContainer" src/components/ui/inventory/InventoryOverlay.module.css</automated>
  </verify>
  <done>
    - `src/components/ui/inventory/InventoryOverlay.module.css` exists
    - `src/components/ui/inventory/InventoryOverlay.module.css` contains `.overlayContainer` and `.splitLayout`
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement Equipment and Backpack Panels</name>
  <files>src/components/ui/inventory/EquipmentPanel.tsx, src/components/ui/inventory/BackpackGrid.tsx, src/components/ui/inventory/EquipmentPanel.test.tsx, src/components/ui/inventory/BackpackGrid.test.tsx</files>
  <behavior>
    - Test 1: EquipmentPanel renders weapon and armor slots with nested software indicators
    - Test 2: BackpackGrid renders correct number of grid slots based on RunInventory
  </behavior>
  <action>
    Create `src/components/ui/inventory/EquipmentPanel.tsx`:
    - Reads `(window as unknown as { gameContext: GameContext }).gameContext`.
    - Gets `playerId` from context.
    - Queries `EquipmentSlots` for `playerId` using `context.world.getComponent(context.playerId, EquipmentSlots)`.
    - Renders the Weapon and Armor slots (placeholder text with entity ID if present).
    - Implement D-03: Add nested indicators (small visual pips/icons) directly on the equipment icon to show installed software slots and their occupancy.

    Create `src/components/ui/inventory/BackpackGrid.tsx`:
    - Reads `gameContext`.
    - Queries `RunInventory` for `playerId`.
    - Creates an array of size `maxSlots` (from the component, or default to 15).
    - Iterates to render empty `.slot` or `.slotFilled` based on items present. Use `inventoryRevision` from `gameStore` via `useStore` to force re-renders.
  </action>
  <verify>
    <automated>npx vitest run src/components/ui/inventory/EquipmentPanel.test.tsx</automated>
  </verify>
  <done>
    - `src/components/ui/inventory/EquipmentPanel.tsx` reads `EquipmentSlots` and implements D-03
    - `src/components/ui/inventory/BackpackGrid.tsx` reads `RunInventory`
    - `src/components/ui/inventory/BackpackGrid.tsx` subscribes to `inventoryRevision` from `gameStore`
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Assemble and Wire Overlay</name>
  <files>src/components/ui/inventory/InventoryOverlay.tsx, src/components/ui/HUDOverlay.tsx, src/components/ui/inventory/InventoryOverlay.test.tsx</files>
  <behavior>
    - Test 1: InventoryOverlay renders both panels
    - Test 2: HUDOverlay conditionally renders InventoryOverlay based on inventoryVisible
  </behavior>
  <action>
    Create `src/components/ui/inventory/InventoryOverlay.tsx`:
    - Combines `EquipmentPanel` and `BackpackGrid` side-by-side using `.splitLayout`.
    - Provides a close button or listens to "Escape" to dispatch `toggleInventory()`.

    Modify `src/components/ui/HUDOverlay.tsx`:
    - Import `InventoryOverlay`.
    - Read `inventoryVisible` from `gameStore`.
    - If `inventoryVisible` is true, render `<InventoryOverlay />` on top of the other HUD elements.
  </action>
  <verify>
    <automated>npx vitest run src/components/ui/inventory/InventoryOverlay.test.tsx</automated>
  </verify>
  <done>
    - `src/components/ui/inventory/InventoryOverlay.tsx` exports default or named component
    - `src/components/ui/HUDOverlay.tsx` imports `InventoryOverlay`
    - `src/components/ui/HUDOverlay.tsx` contains `inventoryVisible` check before rendering `InventoryOverlay`
  </done>
</task>

</tasks>

<verification>
npx tsc --noEmit
</verification>

<success_criteria>
Toggling the inventory displays the overlay with Equipment and Backpack panels populated from the ECS world state, including nested software indicators.
</success_criteria>

<output>
After completion, create `.planning/phases/21-inventory-item-tooltips/21-02-SUMMARY.md`
</output>