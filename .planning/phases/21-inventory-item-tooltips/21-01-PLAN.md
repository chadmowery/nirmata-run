---
phase: 21-inventory-item-tooltips
plan: 1
type: execute
wave: 1
depends_on: []
files_modified:
  - src/game/ui/store.ts
  - src/game/input/actions.ts
  - src/game/setup.ts
  - src/game/ui/sync-bridge.ts
  - src/shared/components/run-inventory.ts
  - src/shared/components/run-inventory.test.ts
  - src/game/ui/store.test.ts
  - src/game/input/actions.test.ts
  - src/game/setup.test.ts
autonomous: true
requirements: [UI-01]
must_haves:
  truths:
    - User can trigger the TOGGLE_INVENTORY action via 'I' key
    - UI state tracks inventory visibility and revision
  artifacts:
    - path: src/game/ui/store.ts
      provides: inventory state fields
    - path: src/shared/components/run-inventory.ts
      provides: gridIndex in schema
  key_links:
    - from: src/game/input/actions.ts
      to: src/game/setup.ts
      via: handlePlayerInput
---

<objective>
Set up the core UI state, action bindings, and schema modifications required for the Inventory Overlay.

Purpose: We need a way to toggle the inventory overlay visibility via input ('I' key), and track revisions to the ECS run inventory so React can properly re-render the HUD.
Output: Store updates, input action integration, and schema extension for manual grid support.
</objective>

<execution_context>
@/Users/chadmowery/personal/dev/dungeon-runner/nimrata-run/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add GridIndex to RunInventory schema</name>
  <files>src/shared/components/run-inventory.ts, src/shared/components/run-inventory.test.ts</files>
  <behavior>
    - Test 1: RunInventoryItemSchema validates correctly with gridIndex
  </behavior>
  <action>
    Modify `src/shared/components/run-inventory.ts`:
    Add `gridIndex: z.number().optional()` to the `RunInventoryItemSchema` object.
    This prepares the backend to store items in specific manual grid slots for D-08.
  </action>
  <verify>
    <automated>npx vitest run src/shared/components/run-inventory.test.ts</automated>
  </verify>
  <done>
    - `src/shared/components/run-inventory.ts` contains `gridIndex: z.number().optional()` inside `RunInventoryItemSchema`
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Expand UI Store with Inventory State</name>
  <files>src/game/ui/store.ts, src/game/ui/store.test.ts</files>
  <behavior>
    - Test 1: store starts with inventoryVisible false
    - Test 2: toggleInventory flips inventoryVisible
  </behavior>
  <action>
    Modify `src/game/ui/store.ts`:
    1. In `UIState` interface, add:
       `inventoryVisible: boolean;`
       `inventoryRevision: number;`
       `toggleInventory: () => void;`
       `incrementInventoryRevision: () => void;`
    2. In `gameStore` defaults, add:
       `inventoryVisible: false,`
       `inventoryRevision: 0,`
    3. In `gameStore` actions, add:
       `toggleInventory: () => set((state) => ({ inventoryVisible: !state.inventoryVisible })),`
       `incrementInventoryRevision: () => set((state) => ({ inventoryRevision: state.inventoryRevision + 1 })),`
    4. In `resetRunStats()`, add:
       `inventoryVisible: false,`
       `inventoryRevision: 0,`
  </action>
  <verify>
    <automated>npx vitest run src/game/ui/store.test.ts</automated>
  </verify>
  <done>
    - `src/game/ui/store.ts` contains `inventoryVisible: boolean`
    - `src/game/ui/store.ts` contains `toggleInventory: () => void`
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Map Input Action to Toggle</name>
  <files>src/game/input/actions.ts, src/game/input/actions.test.ts</files>
  <behavior>
    - Test 1: GameAction enum contains TOGGLE_INVENTORY
  </behavior>
  <action>
    Modify `src/game/input/actions.ts`:
    1. Add `TOGGLE_INVENTORY = 'TOGGLE_INVENTORY',` to the `GameAction` enum.
    2. Add `KeyI: GameAction.TOGGLE_INVENTORY,` to `DEFAULT_BINDINGS`.
  </action>
  <verify>
    <automated>npx vitest run src/game/input/actions.test.ts</automated>
  </verify>
  <done>
    - `src/game/input/actions.ts` contains `TOGGLE_INVENTORY = 'TOGGLE_INVENTORY'`
    - `src/game/input/actions.ts` contains `KeyI: GameAction.TOGGLE_INVENTORY`
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Wire Action to Setup and Bridge</name>
  <files>src/game/setup.ts, src/game/ui/sync-bridge.ts, src/game/setup.test.ts</files>
  <behavior>
    - Test 1: TOGGLE_INVENTORY action calls toggleInventory on gameStore
  </behavior>
  <action>
    Modify `src/game/setup.ts`:
    1. Import `gameStore` at the top: `import { gameStore } from './ui/store';`
    2. Inside `handlePlayerInput(action: GameAction)`, add a handler for the new action at the very top (before the wait/target checks):
       ```typescript
       if (action === GameAction.TOGGLE_INVENTORY) {
         gameStore.getState().toggleInventory();
         return;
       }
       ```
    
    Modify `src/game/ui/sync-bridge.ts`:
    1. Inside `syncEngineToStore`, locate `eventBus.on('RUN_INVENTORY_SYNCED', () => {`
    2. Add `gameStore.getState().incrementInventoryRevision();` inside the callback (right after `refreshPlayerStats()`).
  </action>
  <verify>
    <automated>npx vitest run src/game/setup.test.ts</automated>
  </verify>
  <done>
    - `src/game/setup.ts` contains `if (action === GameAction.TOGGLE_INVENTORY)`
    - `src/game/setup.ts` contains `gameStore.getState().toggleInventory()`
    - `src/game/ui/sync-bridge.ts` contains `gameStore.getState().incrementInventoryRevision()`
  </done>
</task>

</tasks>

<verification>
npx tsc --noEmit
</verification>

<success_criteria>
Input action TOGGLE_INVENTORY correctly mutates the `inventoryVisible` state in `gameStore`.
ECS syncing properly updates `inventoryRevision`.
</success_criteria>

<output>
After completion, create `.planning/phases/21-inventory-item-tooltips/21-01-SUMMARY.md`
</output>