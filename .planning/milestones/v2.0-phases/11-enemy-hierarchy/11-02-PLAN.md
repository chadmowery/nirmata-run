---
phase: 11-enemy-hierarchy
plan: 02
type: execute
wave: 2
depends_on: ["11-01"]
files_modified:
  - src/shared/components/dead-zone.ts
  - src/shared/components/index.ts
  - src/shared/events/types.ts
  - src/game/systems/ai.ts
  - src/game/systems/dead-zone.ts
autonomous: true
requirements: [ENEMY-05, ENEMY-06]

must_haves:
  truths:
    - "Fragmenter creates Dead Zone tiles on ground slam that deal DoT to any entity standing on them"
    - "Dead Zone tiles expire after a configurable number of turns (5-8)"
    - "Logic-Leaker fires ranged attacks when player is in attack range with LOS, kites when player is too close"
    - "Logic-Leaker hit applies FIRMWARE_LOCK status effect to a random Firmware for N turns"
  artifacts:
    - path: "src/shared/components/dead-zone.ts"
      provides: "DeadZone tile marker component with DoT and duration"
      contains: "defineComponent('deadZone'"
      exports: ["DeadZone"]
    - path: "src/game/systems/dead-zone.ts"
      provides: "Dead zone tick system — applies DoT and expires tiles"
      contains: "createDeadZoneSystem"
    - path: "src/game/systems/ai.ts"
      provides: "Fragmenter and Logic-Leaker behavior handlers"
      contains: "processFragmenterTurn"
  key_links:
    - from: "src/game/systems/ai.ts"
      to: "src/shared/components/dead-zone.ts"
      via: "Fragmenter handler creates DeadZone entities"
      pattern: "DeadZone"
    - from: "src/game/systems/ai.ts"
      to: "src/shared/components/status-effects.ts"
      via: "Logic-Leaker applies FIRMWARE_LOCK"
      pattern: "FIRMWARE_LOCK"
---

<objective>
Implement Tier 2 "Static Horrors" enemies: Fragmenter (ground slam creating Dead Zone tiles with DoT) and Logic-Leaker (ranged kiting attacker that locks Firmware on hit).

Purpose: Tier 2 enemies introduce area denial and ranged combat, creating tactical depth on floors 5-10 where players must manage positioning and Firmware availability.
Output: DeadZone component + system, Fragmenter and Logic-Leaker AI handlers in the existing AI system.
</objective>

<execution_context>
@/Users/cmowery/dev/chadspace/nirmata-run-main/.claude/get-shit-done/workflows/execute-plan.md
@/Users/cmowery/dev/chadspace/nirmata-run-main/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/11-enemy-hierarchy/11-CONTEXT.md
@.planning/phases/11-enemy-hierarchy/11-RESEARCH.md
@.planning/phases/11-enemy-hierarchy/11-01-SUMMARY.md

<interfaces>
<!-- From Plan 11-01 (must read SUMMARY for exact signatures) -->

From src/shared/components/ai-state.ts (extended in 11-01):
```typescript
export enum AIBehaviorType {
  BASIC = 'basic',
  NULL_POINTER = 'null_pointer',
  BUFFER_OVERFLOW = 'buffer_overflow',
  FRAGMENTER = 'fragmenter',
  LOGIC_LEAKER = 'logic_leaker',
  SYSTEM_ADMIN = 'system_admin',
  SEED_EATER = 'seed_eater',
}
```

From src/game/systems/ai.ts (refactored in 11-01):
```typescript
// processEnemyTurn now has switch(ai.behaviorType) dispatch
// processFragmenterTurn and processLogicLeakerTurn are stubs — replace them
// Helper functions available: findPlayerEntity(), computeEnemyFov(), isAdjacent(), pathfindToward()
// createAISystem now takes eventBus parameter
```

From src/shared/components/status-effects.ts:
```typescript
export const StatusEffects = defineComponent('statusEffects', z.object({
  effects: z.array(z.object({
    name: z.string(),
    duration: z.number().int().min(0),
    magnitude: z.number().default(0),
    source: z.string().optional(),
  })),
}));
```

From src/shared/components/index.ts:
```typescript
// COMPONENTS_REGISTRY array — add DeadZone here
```

From src/engine/grid/grid.ts:
```typescript
// grid.isWalkable(x, y), grid.addEntity(), grid.removeEntity(), grid.getEntitiesAt(x, y)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: DeadZone Component, Dead Zone System, and New Events</name>
  <files>src/shared/components/dead-zone.ts, src/shared/components/index.ts, src/shared/events/types.ts, src/game/systems/dead-zone.ts</files>
  <read_first>src/shared/components/index.ts, src/shared/events/types.ts, src/shared/components/status-effects.ts, src/engine/grid/grid.ts, src/game/systems/combat.ts</read_first>
  <action>
1. **Create DeadZone component** (`src/shared/components/dead-zone.ts`):
   - `defineComponent('deadZone', z.object({ remainingTurns: z.number().int().min(0), damagePerTick: z.number().default(2), creatorId: z.number().optional() }))`
   - Export `DeadZone` and `DeadZoneData` type
   - DeadZone entities are placed on tiles — they have Position + DeadZone components but no sprite initially (visual added in plan 11-04)

2. **Register component** (`src/shared/components/index.ts`):
   - Import DeadZone from `./dead-zone`
   - Add to COMPONENTS_REGISTRY array
   - Add `export * from './dead-zone'`

3. **Add events** (`src/shared/events/types.ts`):
   - `DEAD_ZONE_CREATED: { x: number; y: number; duration: number; creatorId: EntityId }`
   - `DEAD_ZONE_EXPIRED: { x: number; y: number }`
   - `RANGED_ATTACK: { attackerId: EntityId; defenderId: EntityId; damage: number; projectileType: string }`

4. **Create dead-zone system** (`src/game/systems/dead-zone.ts`):
   - Factory: `createDeadZoneSystem<T extends GameplayEvents>(world: World<T>, grid: Grid, eventBus: EventBus<T>)`
   - `tickDeadZones()` method — called once per turn cycle:
     - Query all entities with DeadZone + Position components
     - For each Dead Zone tile: check all entities at that position via `grid.getEntitiesAt(x, y)`. For each entity that has a Health component, deal `damagePerTick` damage, emit `DAMAGE_DEALT` event
     - Decrement `remainingTurns`. If 0, emit `DEAD_ZONE_EXPIRED`, destroy the DeadZone entity
   - `createDeadZone(x, y, duration, damagePerTick, creatorId)` method — creates a new entity with Position + DeadZone components at the specified tile. Emits `DEAD_ZONE_CREATED` event.
   - Return `{ init(), dispose(), tickDeadZones(), createDeadZone() }`
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - src/shared/components/dead-zone.ts contains `defineComponent('deadZone'` with `remainingTurns`, `damagePerTick`, and `creatorId` fields
    - src/shared/components/index.ts contains `DeadZone` in COMPONENTS_REGISTRY array
    - src/shared/events/types.ts contains `DEAD_ZONE_CREATED:` and `DEAD_ZONE_EXPIRED:` and `RANGED_ATTACK:`
    - src/game/systems/dead-zone.ts contains `createDeadZoneSystem` export
    - src/game/systems/dead-zone.ts contains `tickDeadZones` method that queries DeadZone + Position entities
    - src/game/systems/dead-zone.ts contains `createDeadZone` method that creates an entity with Position and DeadZone components
    - src/game/systems/dead-zone.ts contains damage application via Health component and DAMAGE_DEALT emission
    - `npx tsc --noEmit` exits with code 0
  </acceptance_criteria>
  <done>DeadZone component registered, dead-zone system creates/ticks/expires Dead Zone tiles with DoT damage, 3 new event types added</done>
</task>

<task type="auto">
  <name>Task 2: Fragmenter and Logic-Leaker AI Handlers</name>
  <files>src/game/systems/ai.ts</files>
  <read_first>src/game/systems/ai.ts, src/game/systems/dead-zone.ts, src/shared/components/ai-state.ts, src/shared/components/status-effects.ts, src/shared/components/index.ts, src/shared/events/types.ts</read_first>
  <action>
1. **Update createAISystem signature** to accept the dead-zone system:
   - Add parameter: `deadZoneSystem: { createDeadZone: (x: number, y: number, duration: number, damagePerTick: number, creatorId: EntityId) => void } | null`
   - This parameter is optional (pass null if dead zones not available yet). Guard all dead zone calls with null check.

2. **Fragmenter handler** (`processFragmenterTurn` — replace the stub from 11-01):
   - Per D-10: Fragmenter creates Dead Zone tiles on "ground slam"
   - If player visible AND adjacent: perform ground slam attack
     - Deal bump attack damage normally via movementSystem.processMove
     - Create Dead Zone tiles on up to 4 tiles surrounding the Fragmenter's current position (the 4 cardinal adjacent tiles that are walkable). Duration = 6 turns (configurable, within D-10's 5-8 range). DamagePerTick = 2.
     - Emit `MESSAGE_EMITTED`: "Fragmenter slams the ground! Dead Zones appear!"
   - If player visible AND not adjacent: pathfind toward player (standard chase)
   - If player not visible: IDLE (Fragmenter is slow per D-03, energy speed 60, doesn't actively patrol)
   - Stats (discretionary): sightRadius=5, energy speed=60 (per D-03 — slow)

3. **Logic-Leaker handler** (`processLogicLeakerTurn` — replace the stub from 11-01):
   - Per D-08: ranged attack with LOS, kites when too close
   - Define range constants: `ATTACK_RANGE = 5`, `KITE_RANGE = 2` (per Pitfall 4 from research)
   - If player visible:
     - Calculate Manhattan distance to player
     - If distance <= KITE_RANGE (too close): move AWAY from player. Calculate opposite direction vector, pathfind to tile 3 steps away from player. Emit `MESSAGE_EMITTED`: "Logic-Leaker retreats!"
     - If distance > KITE_RANGE AND distance <= ATTACK_RANGE AND has LOS: fire ranged attack
       - Emit `RANGED_ATTACK` event with `{ attackerId: entityId, defenderId: player.id, damage: 5, projectileType: 'corrupted_packet' }`
       - Deal damage directly: get player's Health component, subtract damage (respecting Defense), emit `DAMAGE_DEALT`
       - On hit: apply `FIRMWARE_LOCK` status effect per D-09. Get player entity's StatusEffects, push `{ name: 'FIRMWARE_LOCK', duration: 3, magnitude: 1, source: 'logic_leaker' }`. Emit `STATUS_EFFECT_APPLIED`.
       - Emit `MESSAGE_EMITTED`: "Logic-Leaker fires a Corrupted Packet! Firmware locked!"
     - If distance > ATTACK_RANGE: pathfind toward player (close the gap)
   - If player not visible: IDLE
   - Stats (discretionary): sightRadius=7, energy speed=90 (moderately fast ranged attacker)
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - src/game/systems/ai.ts contains `processFragmenterTurn` function (not a stub/console.warn)
    - src/game/systems/ai.ts Fragmenter handler calls `deadZoneSystem.createDeadZone` with duration and damagePerTick arguments
    - src/game/systems/ai.ts Fragmenter creates Dead Zones on cardinal adjacent tiles (checks walkability before placing)
    - src/game/systems/ai.ts contains `processLogicLeakerTurn` function (not a stub/console.warn)
    - src/game/systems/ai.ts Logic-Leaker handler contains `KITE_RANGE` and `ATTACK_RANGE` distance checks
    - src/game/systems/ai.ts Logic-Leaker applies `FIRMWARE_LOCK` status effect with `duration: 3` on successful ranged hit
    - src/game/systems/ai.ts Logic-Leaker emits `RANGED_ATTACK` event with `projectileType: 'corrupted_packet'`
    - src/game/systems/ai.ts Logic-Leaker kites (moves away) when distance <= KITE_RANGE
    - `npx tsc --noEmit` exits with code 0
  </acceptance_criteria>
  <done>Fragmenter creates Dead Zone tiles on slam with 6-turn expiry and 2 DoT, Logic-Leaker fires at range with FIRMWARE_LOCK and kites when player closes distance, both behaviors fully implemented</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes
- `npm test -- --run` all existing tests pass
- Fragmenter handler creates Dead Zone entities with Position + DeadZone components
- Logic-Leaker correctly differentiates between kite range, attack range, and chase range
- FIRMWARE_LOCK status effect applied with correct duration on Logic-Leaker hit
</verification>

<success_criteria>
- Fragmenter ground slam creates time-limited Dead Zone tiles (6 turns) on adjacent walkable tiles dealing 2 damage/turn
- Logic-Leaker fires ranged Corrupted Packet at 2-5 tile range, kites below range 2, chases beyond range 5
- Logic-Leaker hit applies FIRMWARE_LOCK for 3 turns to player's StatusEffects
- Dead zone system ticks down durations and cleans up expired zones
</success_criteria>

<output>
After completion, create `.planning/phases/11-enemy-hierarchy/11-02-SUMMARY.md`
</output>
