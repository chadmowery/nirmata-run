---
wave: 1
depends_on: []
files_modified:
  - src/shared/components/shell.ts
  - src/shared/components/port-config.ts
  - src/shared/components/firmware-slots.ts
  - src/shared/components/augment-slots.ts
  - src/shared/components/software-slots.ts
  - src/shared/components/index.ts
  - src/game/shells/types.ts
  - src/game/shells/shell-registry.ts
  - src/game/shells/templates/striker-v1.json
  - src/game/shells/templates/bastion-v1.json
  - src/game/shells/templates/signal-v1.json
  - src/shared/components/__tests__/shell.test.ts
  - src/shared/components/__tests__/port-config.test.ts
  - src/game/shells/__tests__/shell-registry.test.ts
  - src/game/shells/__tests__/template-loading.test.ts
autonomous: true
requirements: ["SHELL-01", "SHELL-02", "SHELL-07"]
---

# Phase 7 - Plan 01: Shell Components & Entity Templates

<objective>
To implement the core data models for Shells, Port Configurations, and Equipment Slots, establish the ShellRegistry, and create the three starter Shell archetypes as JSON templates.
</objective>

<task type="tdd">
  <read_first>
    - src/engine/ecs/types.ts
    - src/shared/components/health.ts
  </read_first>
  <action>
    Create `src/shared/components/shell.ts` with a `ShellComponent` defining base stats.
    Define a Zod schema `z.object({ speed: z.number().int().positive(), stability: z.number().int().min(0), armor: z.number().int().min(0), maxHealth: z.number().int().positive() })`.
    Export the defined component as `Shell` and infer its type as `ShellData`.
    Include tests in `src/shared/components/__tests__/shell.test.ts` to verify the schema validation.
  </action>
  <acceptance_criteria>
    - `grep -q "export const Shell = defineComponent" src/shared/components/shell.ts` exits 0
    - `npx vitest run src/shared/components/__tests__/shell.test.ts` exits 0
  </acceptance_criteria>
</task>

<task type="tdd">
  <read_first>
    - src/engine/ecs/types.ts
  </read_first>
  <action>
    Create `src/shared/components/port-config.ts` for `PortConfigComponent`.
    Schema: `z.object({ maxFirmware: z.number().int().min(0), maxAugment: z.number().int().min(0), maxSoftware: z.number().int().min(0) })`.
    Export `PortConfig` and `PortConfigData`.
    Include tests in `src/shared/components/__tests__/port-config.test.ts` to ensure slot counts are non-negative integers.
  </action>
  <acceptance_criteria>
    - `grep -q "export const PortConfig = defineComponent" src/shared/components/port-config.ts` exits 0
    - `npx vitest run src/shared/components/__tests__/port-config.test.ts` exits 0
  </acceptance_criteria>
</task>

<task type="auto">
  <read_first>
    - src/engine/ecs/types.ts
    - src/shared/components/index.ts
  </read_first>
  <action>
    Create three equipment slot components for Phase 7:
    1. `src/shared/components/firmware-slots.ts` - `FirmwareSlots = defineComponent('firmwareSlots', z.object({ equipped: z.array(z.number()) }))`
    2. `src/shared/components/augment-slots.ts` - `AugmentSlots = defineComponent('augmentSlots', z.object({ equipped: z.array(z.number()) }))`
    3. `src/shared/components/software-slots.ts` - `SoftwareSlots = defineComponent('softwareSlots', z.object({ equipped: z.array(z.number()) }))`
    Update `src/shared/components/index.ts` to include `Shell`, `PortConfig`, `FirmwareSlots`, `AugmentSlots`, and `SoftwareSlots` in `COMPONENTS_REGISTRY` and export them.
  </action>
  <acceptance_criteria>
    - `grep -q "export const FirmwareSlots =" src/shared/components/firmware-slots.ts` exits 0
    - `grep -q "export const AugmentSlots =" src/shared/components/augment-slots.ts` exits 0
    - `grep -q "export const SoftwareSlots =" src/shared/components/software-slots.ts` exits 0
    - `grep -q "Shell," src/shared/components/index.ts` exits 0
  </acceptance_criteria>
</task>

<task type="tdd">
  <read_first>
    - src/engine/entity/registry.ts
  </read_first>
  <action>
    Create `src/game/shells/types.ts` defining `ShellStats`, `PortConfig`, `ShellUpgrade` representing stat/port bonuses per level, and `ShellTemplate` for raw JSON representation.
    Define `ShellRecord` interface with `id: string`, `archetypeId: string`, `level: number`, `currentStats: ShellStats`, and `portConfig: PortConfig`.
    Create `src/game/shells/shell-registry.ts` defining `ShellRegistry` class (pattern from `EntityRegistry`). 
    Methods: `register(template: ShellTemplate)`, `get(id: string)`, `has(id: string)`, `getAll()`, `update(id: string, updates: Partial<ShellRecord>)`.
    Implement unit tests for CRUD ops in `src/game/shells/__tests__/shell-registry.test.ts`.
  </action>
  <acceptance_criteria>
    - `grep -q "export interface ShellRecord" src/game/shells/types.ts` exits 0
    - `grep -q "export class ShellRegistry" src/game/shells/shell-registry.ts` exits 0
    - `npx vitest run src/game/shells/__tests__/shell-registry.test.ts` exits 0
  </acceptance_criteria>
</task>

<task type="tdd">
  <read_first>
    - src/game/shells/shell-registry.ts
  </read_first>
  <action>
    Create starter shell templates in `src/game/shells/templates/`:
    1. `striker-v1.json`: `{ "id": "striker-v1", "name": "STRIKER-v1", "baseStats": { "speed": 120, "stability": 5, "armor": 1, "maxHealth": 15 }, "basePorts": { "maxFirmware": 2, "maxAugment": 1, "maxSoftware": 2 }, "upgrades": [] }`
    2. `bastion-v1.json`: `{ "id": "bastion-v1", "name": "BASTION-v1", "baseStats": { "speed": 80, "stability": 10, "armor": 3, "maxHealth": 25 }, "basePorts": { "maxFirmware": 1, "maxAugment": 1, "maxSoftware": 1 }, "upgrades": [] }`
    3. `signal-v1.json`: `{ "id": "signal-v1", "name": "SIGNAL-v1", "baseStats": { "speed": 100, "stability": 8, "armor": 1, "maxHealth": 18 }, "basePorts": { "maxFirmware": 2, "maxAugment": 2, "maxSoftware": 3 }, "upgrades": [] }`
    Create tests in `src/game/shells/__tests__/template-loading.test.ts` to ensure these JSON files are correctly parsed into `ShellTemplate` and registered into `ShellRegistry`.
  </action>
  <acceptance_criteria>
    - `test -f src/game/shells/templates/striker-v1.json` exits 0
    - `test -f src/game/shells/templates/bastion-v1.json` exits 0
    - `test -f src/game/shells/templates/signal-v1.json` exits 0
    - `npx vitest run src/game/shells/__tests__/template-loading.test.ts` exits 0
  </acceptance_criteria>
</task>
