---
phase: 14-stash-vault-run-modes
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/game/systems/run-mode-config.ts
  - src/game/systems/run-mode-config.test.ts
  - src/game/systems/seed-rotation.ts
  - src/game/systems/seed-rotation.test.ts
  - src/game/entities/templates/economy.json
  - data/seeds/rotation.json
autonomous: true
requirements:
  - RUN-01
  - RUN-04
  - RUN-05
  - RUN-06

must_haves:
  truths:
    - "Three run modes (simulation, daily, weekly) have distinct config objects with correct rules"
    - "Simulation uses random seed, unlimited attempts, real gear, no leaderboard"
    - "Daily/Weekly use server-generated shared seeds from rotation file"
    - "Each mode specifies correct death penalties (equipment loss for all; shell upgrade reset for weekly only)"
    - "Scoring formula weights are configurable in economy.json"
  artifacts:
    - path: "src/game/systems/run-mode-config.ts"
      provides: "RunMode enum, RunModeConfig interface, mode config factories"
      exports: ["RunMode", "RunModeConfig", "getRunModeConfig"]
    - path: "src/game/systems/seed-rotation.ts"
      provides: "Seed generation, rotation storage, daily/weekly seed retrieval"
      exports: ["getCurrentDailySeed", "getCurrentWeeklySeed", "rotateSeed", "loadSeedRotation", "saveSeedRotation"]
    - path: "src/game/entities/templates/economy.json"
      provides: "Scoring formula weights"
      contains: "scoring"
    - path: "data/seeds/rotation.json"
      provides: "Server-generated seed rotation storage"
  key_links:
    - from: "src/game/systems/run-mode-config.ts"
      to: "src/game/systems/seed-rotation.ts"
      via: "getRunModeConfig calls getCurrentDailySeed/getCurrentWeeklySeed"
      pattern: "getCurrentDailySeed|getCurrentWeeklySeed"
---

<objective>
Create run mode configuration system with seed rotation for deterministic competitive runs.

Purpose: The three run modes (Simulation, Daily Challenge, Weekly One-Shot) need distinct rule sets defining stakes, seeds, and scoring. Server-generated seeds ensure competitive fairness.
Output: RunModeConfig module, SeedRotation module, scoring weights in economy.json, seed rotation storage.
</objective>

<execution_context>
@/Users/cmowery/dev/chadspace/nirmata-run-main/.claude/get-shit-done/workflows/execute-plan.md
@/Users/cmowery/dev/chadspace/nirmata-run-main/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/14-stash-vault-run-modes/14-CONTEXT.md
@.planning/phases/14-stash-vault-run-modes/14-RESEARCH.md

<interfaces>
From src/game/engine-factory.ts:
```typescript
export interface EngineInitConfig {
  width: number;
  height: number;
  seed: string;
  isClient?: boolean;
  shellRecord?: ShellRecord;
  sessionId?: string;
  profile?: PlayerProfile;
}
```

From src/game/systems/shop-rotation.ts (mulberry32 pattern):
```typescript
function seededRandom(seed: number): () => number {
  return () => {
    seed |= 0; seed = seed + 0x6D2B79F5 | 0;
    let t = Math.imul(seed ^ seed >>> 15, 1 | seed);
    t = t + Math.imul(t ^ t >>> 7, 61 | t) ^ t;
    return ((t ^ t >>> 14) >>> 0) / 4294967296;
  };
}
```

From src/game/entities/templates/economy.json:
```json
{
  "caps": { "scrap": 10000, "flux": 1000 },
  "pity": { "deathScrapPercent": 0.25 },
  "shop": { "stockSize": 6, "maxRarity": "rare" }
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RunModeConfig module and scoring weights</name>
  <files>src/game/systems/run-mode-config.ts, src/game/systems/run-mode-config.test.ts, src/game/entities/templates/economy.json</files>
  <read_first>
    - src/game/engine-factory.ts
    - src/game/entities/templates/economy.json
    - src/game/systems/shop-rotation.ts
    - src/game/systems/profile-persistence.ts
  </read_first>
  <behavior>
    - Test: RunMode enum has values 'simulation', 'daily', 'weekly'
    - Test: getRunModeConfig(RunMode.SIMULATION) returns { mode: 'simulation', seed: (random string), isLeaderboardEligible: false, deathPenalty: { loseEquipment: true, resetShellUpgrades: false } }
    - Test: getRunModeConfig(RunMode.DAILY) returns { mode: 'daily', seed: (daily seed), isLeaderboardEligible: true, deathPenalty: { loseEquipment: true, resetShellUpgrades: false } }
    - Test: getRunModeConfig(RunMode.WEEKLY) returns { mode: 'weekly', seed: (weekly seed), isLeaderboardEligible: true, deathPenalty: { loseEquipment: true, resetShellUpgrades: true } }
    - Test: calculateScore({ depth: 10, kills: 30, lootValue: 500, turns: 200 }) returns a positive integer using weights from economy.json
    - Test: calculateScore with 0 turns does not divide by zero (Math.max(turns, 1))
    - Test: RunModeConfig interface has mode, seed, isLeaderboardEligible, deathPenalty fields
  </behavior>
  <action>
    1. Create src/game/systems/run-mode-config.ts:
       - export enum RunMode { SIMULATION = 'simulation', DAILY = 'daily', WEEKLY = 'weekly' }
       - export interface RunModeConfig { mode: RunMode; seed: string; isLeaderboardEligible: boolean; deathPenalty: { loseEquipment: boolean; resetShellUpgrades: boolean; }; }
       - export interface RunStats { depth: number; kills: number; lootValue: number; turns: number; }
       - export async function getRunModeConfig(mode: RunMode): Promise<RunModeConfig>
         - SIMULATION: seed = crypto.randomUUID(), isLeaderboardEligible = false, deathPenalty = { loseEquipment: true, resetShellUpgrades: false } (per D-09, D-10, D-22)
         - DAILY: seed = await getCurrentDailySeed(), isLeaderboardEligible = true, deathPenalty = { loseEquipment: true, resetShellUpgrades: false } (per D-12)
         - WEEKLY: seed = await getCurrentWeeklySeed(), isLeaderboardEligible = true, deathPenalty = { loseEquipment: true, resetShellUpgrades: true } (per D-12, D-16)
       - export function calculateScore(stats: RunStats): number
         - Load weights from economy.json scoring section
         - depthScore = stats.depth * weights.depthMultiplier
         - killScore = stats.kills * weights.killMultiplier
         - lootScore = stats.lootValue * weights.lootMultiplier
         - speedScore = (weights.maxTurns / Math.max(stats.turns, 1)) * weights.speedMultiplier
         - return Math.floor(depthScore + killScore + lootScore + speedScore)

    2. Add scoring weights to economy.json (add new top-level "scoring" key):
       ```json
       "scoring": {
         "depthMultiplier": 100,
         "killMultiplier": 10,
         "lootMultiplier": 1,
         "speedMultiplier": 500,
         "maxTurns": 1000
       }
       ```

    3. Write run-mode-config.test.ts with TDD (RED then GREEN).
  </action>
  <verify>
    <automated>npm test -- src/game/systems/run-mode-config.test.ts -x</automated>
  </verify>
  <acceptance_criteria>
    - run-mode-config.ts exports `RunMode` enum with values 'simulation', 'daily', 'weekly'
    - run-mode-config.ts exports `RunModeConfig` interface with mode, seed, isLeaderboardEligible, deathPenalty
    - run-mode-config.ts exports `RunStats` interface with depth, kills, lootValue, turns
    - run-mode-config.ts exports `getRunModeConfig` and `calculateScore`
    - economy.json contains `"scoring"` key with depthMultiplier, killMultiplier, lootMultiplier, speedMultiplier, maxTurns
    - calculateScore uses Math.max(stats.turns, 1) to prevent division by zero
    - Weekly mode config has resetShellUpgrades: true, others have false
    - Simulation mode has isLeaderboardEligible: false, others true
    - run-mode-config.test.ts exits 0
  </acceptance_criteria>
  <done>Run mode configurations defined for all three modes with correct stakes per D-12. Scoring formula configurable via economy.json.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Seed rotation storage and retrieval</name>
  <files>src/game/systems/seed-rotation.ts, src/game/systems/seed-rotation.test.ts, data/seeds/rotation.json</files>
  <read_first>
    - src/game/systems/shop-rotation.ts
    - src/game/systems/profile-persistence.ts
    - src/game/entities/templates/economy.json
  </read_first>
  <behavior>
    - Test: SeedRotationSchema validates { daily: { currentDayNumber, seed, generatedAt }, weekly: { currentWeekNumber, seed, generatedAt } }
    - Test: getCurrentDailySeed() returns a hex string seed; calling twice on same day returns same seed
    - Test: getCurrentDailySeed() generates new seed when day number changes
    - Test: getCurrentWeeklySeed() returns a hex string seed; calling twice in same week returns same seed
    - Test: getCurrentWeeklySeed() generates new seed when week number changes
    - Test: rotateSeed('daily') forces a new daily seed regardless of day
    - Test: rotateSeed('weekly') forces a new weekly seed regardless of week
    - Test: getDayNumber() returns Math.floor(Date.now() / 86400000) (days since epoch)
    - Test: getWeekNumber() returns a consistent week number
    - Test: loadSeedRotation() returns default values when file does not exist
  </behavior>
  <action>
    1. Create src/game/systems/seed-rotation.ts:
       - Import { z } from 'zod', fs from 'fs/promises', path, crypto
       - SeedRotationSchema = z.object({ daily: z.object({ currentDayNumber: z.number().default(0), seed: z.string().default(''), generatedAt: z.number().default(0) }).default({}), weekly: z.object({ currentWeekNumber: z.number().default(0), seed: z.string().default(''), generatedAt: z.number().default(0) }).default({}) })
       - export type SeedRotation = z.infer<typeof SeedRotationSchema>
       - SEEDS_DIR = path.join(process.cwd(), 'data', 'seeds')
       - ROTATION_PATH = path.join(SEEDS_DIR, 'rotation.json')
       - generateRandomSeed(): string — return crypto.randomBytes(16).toString('hex') (per D-18: NOT date-derived)
       - getDayNumber(): number — Math.floor(Date.now() / (1000 * 60 * 60 * 24))
       - getWeekNumber(): number — Math.floor(Date.now() / (1000 * 60 * 60 * 24 * 7))
       - loadSeedRotation(): Promise<SeedRotation> — read file, parse with schema, return defaults if ENOENT
       - saveSeedRotation(rotation: SeedRotation): Promise<void> — mkdir -p, temp file + rename (atomic write pattern from profile-persistence.ts)
       - getCurrentDailySeed(): Promise<string> — load rotation, check if rotation.daily.currentDayNumber !== getDayNumber(), if stale generate new seed + save, return seed
       - getCurrentWeeklySeed(): Promise<string> — same logic for weekly using getWeekNumber()
       - rotateSeed(period: 'daily' | 'weekly'): Promise<string> — force generate new seed for period, save, return new seed

    2. Create initial data/seeds/rotation.json:
       ```json
       { "daily": { "currentDayNumber": 0, "seed": "", "generatedAt": 0 }, "weekly": { "currentWeekNumber": 0, "seed": "", "generatedAt": 0 } }
       ```

    3. Write seed-rotation.test.ts with TDD:
       - Mock fs operations for isolated testing
       - Mock Date.now() and crypto.randomBytes for determinism
       - Test all behaviors listed above
  </action>
  <verify>
    <automated>npm test -- src/game/systems/seed-rotation.test.ts -x</automated>
  </verify>
  <acceptance_criteria>
    - seed-rotation.ts exports `getCurrentDailySeed`, `getCurrentWeeklySeed`, `rotateSeed`, `loadSeedRotation`, `saveSeedRotation`, `getDayNumber`, `getWeekNumber`
    - seed-rotation.ts contains `crypto.randomBytes(16).toString('hex')` for seed generation (NOT date hash per D-18)
    - seed-rotation.ts uses atomic write pattern (temp file + rename)
    - data/seeds/rotation.json exists with daily/weekly structure
    - seed-rotation.test.ts exits 0 with all tests passing
    - Same-day calls return identical daily seed
    - Same-week calls return identical weekly seed
  </acceptance_criteria>
  <done>Server-generated seed rotation system stores and retrieves daily/weekly seeds. Seeds are random (not date-derived per D-18). Auto-rotates on period change.</done>
</task>

</tasks>

<verification>
- npm test -- src/game/systems/run-mode-config.test.ts src/game/systems/seed-rotation.test.ts -x
- npx tsc --noEmit
- economy.json contains scoring weights
- data/seeds/rotation.json exists
</verification>

<success_criteria>
- Three run mode configs with correct stakes, seeds, and leaderboard eligibility
- Seed rotation generates and persists daily/weekly seeds server-side
- Scoring formula is configurable via economy.json
- All tests pass, no type errors
</success_criteria>

<output>
After completion, create `.planning/phases/14-stash-vault-run-modes/14-02-SUMMARY.md`
</output>
