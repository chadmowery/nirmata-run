---
phase: 14-stash-vault-run-modes
plan: 04
type: execute
wave: 2
depends_on: ["14-01", "14-02"]
files_modified:
  - src/app/api/vault/move-to-vault/route.ts
  - src/app/api/vault/equip-from-vault/route.ts
  - src/app/api/admin/seed-rotation/route.ts
  - src/app/api/admin/reset-attempts/route.ts
  - src/game/systems/weekly-reset.ts
  - src/game/systems/__tests__/run-mode-integration.test.ts
autonomous: true
requirements:
  - RUN-02
  - RUN-03
  - RUN-07

must_haves:
  truths:
    - "Players can move overflow items to Vault (clear overflow before next run)"
    - "Players can equip items from Vault onto Shell as pre-run Ritual (API-only per D-14)"
    - "Admin can manually rotate seeds and reset attempt tracking"
    - "Weekly reset (Format C:) also resets weekly attempt tracking (D-15)"
    - "Integration test verifies full run lifecycle: launch -> extract -> overflow -> clear -> relaunch"
  artifacts:
    - path: "src/app/api/vault/move-to-vault/route.ts"
      provides: "POST endpoint to move overflow items to Vault"
      exports: ["POST"]
    - path: "src/app/api/vault/equip-from-vault/route.ts"
      provides: "POST endpoint for Ritual equip-from-Vault action"
      exports: ["POST"]
    - path: "src/app/api/admin/seed-rotation/route.ts"
      provides: "GET/POST endpoints for seed rotation management"
      exports: ["GET", "POST"]
    - path: "src/app/api/admin/reset-attempts/route.ts"
      provides: "POST endpoint to reset attempt tracking"
      exports: ["POST"]
  key_links:
    - from: "src/app/api/vault/move-to-vault/route.ts"
      to: "src/game/systems/vault-manager.ts"
      via: "moveOverflowToVault for batch move"
      pattern: "moveOverflowToVault"
    - from: "src/app/api/vault/equip-from-vault/route.ts"
      to: "src/game/systems/profile-persistence.ts"
      via: "loadProfile/saveProfile for Vault-to-Shell equipment transfer"
      pattern: "profile.vault.*profile.installedItems"
    - from: "src/game/systems/weekly-reset.ts"
      to: "src/game/systems/profile-persistence.ts"
      via: "Reset attemptTracking fields during Format C:"
      pattern: "attemptTracking"
---

<objective>
Complete the Vault-to-Shell Ritual flow, admin tooling for seed/attempt management, and weekly reset integration for attempt tracking.

Purpose: Players need to clear overflow and equip items from Vault before runs (universal Ritual per D-13). Admins need seed rotation and attempt reset controls. Weekly reset must also clear attempt tracking.
Output: Vault move/equip endpoints, admin seed/attempt endpoints, weekly reset extension, integration test.
</objective>

<execution_context>
@/Users/cmowery/dev/chadspace/nirmata-run-main/.claude/get-shit-done/workflows/execute-plan.md
@/Users/cmowery/dev/chadspace/nirmata-run-main/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/14-stash-vault-run-modes/14-CONTEXT.md
@.planning/phases/14-stash-vault-run-modes/14-RESEARCH.md
@.planning/phases/14-stash-vault-run-modes/14-01-SUMMARY.md
@.planning/phases/14-stash-vault-run-modes/14-02-SUMMARY.md

<interfaces>
From src/game/systems/vault-manager.ts (from Plan 14-01):
```typescript
export const VAULT_MAX_SLOTS = 30;
export function moveOverflowToVault(sessionId: string): Promise<{moved: number, remaining: number}>;
export function hasOverflow(sessionId: string): Promise<boolean>;
export function inferItemType(templateId: string): 'firmware' | 'augment' | 'software';
```

From src/game/systems/profile-persistence.ts (extended in Plan 14-01):
```typescript
export type VaultItem = z.infer<typeof VaultItemSchema>;
// VaultItem: { entityId, templateId, rarityTier, itemType, extractedAtFloor, extractedAtTimestamp }
// Profile has: vault: VaultItem[], overflow: VaultItem[], attemptTracking: { weekNumber, weeklyAttemptUsed, dayNumber, dailyAttemptUsed }
// Profile has: installedItems: InstalledItem[] — { blueprintId, type, shellId, isLegacy }
```

From src/game/systems/seed-rotation.ts (from Plan 14-02):
```typescript
export async function loadSeedRotation(): Promise<SeedRotation>;
export async function rotateSeed(period: 'daily' | 'weekly'): Promise<string>;
```

From src/game/systems/weekly-reset.ts:
```typescript
export async function executeWeeklyReset(sessionId: string, newWeekSeed: number): Promise<ResetResult>;
// Currently: deletes uninstalled blueprints, applies legacy, resets shell upgrades, caps currency, updates weekSeed
```

From src/app/api/economy/compile/route.ts (API pattern):
```typescript
// Pattern: Zod schema, loadProfile, mutation, saveProfile, NextResponse.json
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Vault move-to-vault, equip-from-vault, and admin endpoints</name>
  <files>src/app/api/vault/move-to-vault/route.ts, src/app/api/vault/equip-from-vault/route.ts, src/app/api/admin/seed-rotation/route.ts, src/app/api/admin/reset-attempts/route.ts</files>
  <read_first>
    - src/game/systems/vault-manager.ts
    - src/game/systems/profile-persistence.ts
    - src/game/systems/seed-rotation.ts
    - src/app/api/economy/compile/route.ts
    - src/app/api/admin/reset/route.ts
    - src/game/entities/templates/economy.json
  </read_first>
  <action>
    1. Create src/app/api/vault/move-to-vault/route.ts (POST):
       - Zod: MoveRequestSchema = z.object({ sessionId: z.string() })
       - Call moveOverflowToVault(sessionId)
       - Return { success: true, moved: number, remaining: number, vaultCount: profile.vault.length }
       - If remaining > 0, include message: 'VAULT_CAPACITY_REACHED — discard or sell remaining items'

    2. Create src/app/api/vault/equip-from-vault/route.ts (POST) — Ritual API per D-13, D-14:
       - Zod: EquipRequestSchema = z.object({ sessionId: z.string(), vaultItemEntityId: z.number(), shellId: z.string(), slotType: z.enum(['firmware', 'augment']) })
       - Load profile; 404 if not found
       - Find item in profile.vault by entityId; 404 if not found
       - Validate item.itemType matches slotType; 400 if mismatch
       - Remove item from vault array
       - Add to profile.installedItems: { blueprintId: item.templateId, type: item.itemType as 'firmware' | 'augment', shellId, isLegacy: false }
       - Save profile
       - Return { success: true, installedItems: profile.installedItems.length, vaultCount: profile.vault.length }
       - NOTE: Software cannot be equipped via this endpoint (Software is burned during combat, not installed between runs). Only firmware/augment allowed.

    3. Create src/app/api/admin/seed-rotation/route.ts:
       - GET handler: load seed rotation, return { daily: { currentDayNumber, seed, generatedAt }, weekly: { currentWeekNumber, seed, generatedAt } }
       - POST handler: Zod { period: z.enum(['daily', 'weekly']) }, call rotateSeed(period), return { success: true, newSeed, period }

    4. Create src/app/api/admin/reset-attempts/route.ts (POST):
       - Zod: ResetAttemptsSchema = z.object({ sessionId: z.string().optional(), period: z.enum(['daily', 'weekly']) })
       - If sessionId provided: load single profile, reset that period's attempt flag, save
       - If no sessionId: read all profile files from data/profiles/, reset attempt flag on each, save each
       - Return { success: true, affectedProfiles: count }
       - Daily reset: set dailyAttemptUsed = false
       - Weekly reset: set weeklyAttemptUsed = false
  </action>
  <verify>
    <automated>npx tsc --noEmit --pretty 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - src/app/api/vault/move-to-vault/route.ts contains `export async function POST` and calls `moveOverflowToVault`
    - src/app/api/vault/equip-from-vault/route.ts contains `export async function POST`
    - src/app/api/vault/equip-from-vault/route.ts validates `slotType: z.enum(['firmware', 'augment'])` (no software)
    - src/app/api/vault/equip-from-vault/route.ts removes item from vault and adds to installedItems
    - src/app/api/admin/seed-rotation/route.ts exports GET and POST handlers
    - src/app/api/admin/seed-rotation/route.ts POST calls `rotateSeed(period)`
    - src/app/api/admin/reset-attempts/route.ts contains `dailyAttemptUsed = false` or `weeklyAttemptUsed = false`
    - TypeScript compilation passes
  </acceptance_criteria>
  <done>Vault overflow-to-vault transfer, equip-from-vault Ritual, and admin seed/attempt management endpoints complete.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Weekly reset attempt tracking extension and integration test</name>
  <files>src/game/systems/weekly-reset.ts, src/game/systems/__tests__/run-mode-integration.test.ts</files>
  <read_first>
    - src/game/systems/weekly-reset.ts
    - src/game/systems/profile-persistence.ts
    - src/game/systems/vault-manager.ts
    - src/game/systems/run-mode-config.ts
    - src/game/systems/seed-rotation.ts
    - src/app/api/run-mode/launch/route.ts
  </read_first>
  <behavior>
    - Test: executeWeeklyReset clears weeklyAttemptUsed to false and updates weekNumber
    - Test: Full lifecycle: create profile -> launch simulation (succeeds) -> extract (items in overflow) -> move to vault -> launch daily (succeeds, marks attempt) -> launch daily again (fails 403)
    - Test: Weekly mode launch marks weeklyAttemptUsed=true; second attempt rejected
    - Test: Day number change resets dailyAttemptUsed automatically on next launch
    - Test: Overflow blocks all run launches until resolved
  </behavior>
  <action>
    1. Update weekly-reset.ts executeWeeklyReset():
       - After existing reset logic (blueprint deletion, legacy, shell upgrades, currency caps, weekSeed):
       - Add: profile.attemptTracking.weeklyAttemptUsed = false (per D-15: weekly attempt resets on Format C:)
       - Add: profile.attemptTracking.weekNumber = newWeekSeed (or use getWeekNumber() — the key is that after reset, the player can attempt the new week)
       - Do NOT reset dailyAttemptUsed here — daily resets happen naturally via day number change
       - Update ResetResult interface if needed to include attemptReset: boolean field

    2. Create src/game/systems/__tests__/run-mode-integration.test.ts:
       - Integration test that exercises the full run lifecycle without actual engine creation:
       - Test "simulation launch always succeeds":
         - Create test profile with no overflow, no attempts used
         - Verify launch returns success with random seed
       - Test "overflow blocks launch":
         - Create profile with overflow items
         - Verify launch returns 400 VAULT_OVERFLOW_UNRESOLVED
         - Clear overflow, verify launch succeeds
       - Test "daily attempt tracking":
         - Launch daily (succeeds, marks dailyAttemptUsed)
         - Launch daily again same day (fails 403)
         - Simulate day change (mock getDayNumber to return different value)
         - Launch daily (succeeds)
       - Test "weekly attempt tracking with reset":
         - Launch weekly (succeeds, marks weeklyAttemptUsed)
         - Launch weekly again (fails 403)
         - Execute weekly reset
         - Launch weekly (succeeds)
       - Test "weekly death resets shell upgrades":
         - Create profile with shellUpgrades
         - Simulate weekly death (call profile mutation that run-ender would do)
         - Verify shellUpgrades = {}
       - Mock fs operations and Date.now() for deterministic tests
  </action>
  <verify>
    <automated>npm test -- src/game/systems/__tests__/run-mode-integration.test.ts -x</automated>
  </verify>
  <acceptance_criteria>
    - weekly-reset.ts contains `profile.attemptTracking.weeklyAttemptUsed = false`
    - weekly-reset.ts sets attemptTracking.weekNumber during reset
    - run-mode-integration.test.ts contains at least 5 test cases covering simulation launch, overflow blocking, daily attempts, weekly attempts with reset, and weekly death shell reset
    - run-mode-integration.test.ts exits 0 with all tests passing
  </acceptance_criteria>
  <done>Weekly reset clears attempt tracking. Integration tests verify full run lifecycle including overflow gating, attempt enforcement, and mode-specific death handling.</done>
</task>

</tasks>

<verification>
- npm test -- src/game/systems/__tests__/run-mode-integration.test.ts -x
- npx tsc --noEmit
- Verify weekly-reset.ts resets attemptTracking
- Verify all 4 new API routes exist and export correct handlers
</verification>

<success_criteria>
- Vault move-to-vault endpoint clears overflow
- Equip-from-vault Ritual endpoint moves items from Vault to Shell (firmware/augment only)
- Admin can rotate seeds and reset attempts
- Weekly reset also clears attempt tracking
- Integration test covers full run lifecycle
- All tests pass, TypeScript compiles
</success_criteria>

<output>
After completion, create `.planning/phases/14-stash-vault-run-modes/14-04-SUMMARY.md`
</output>
