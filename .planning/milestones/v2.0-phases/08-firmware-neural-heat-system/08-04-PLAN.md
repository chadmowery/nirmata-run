---
wave: 3
depends_on: ["08-01", "08-02", "08-03"]
files_modified:
  - src/game/entities/templates/phase-shift.json
  - src/game/entities/templates/neural-spike.json
  - src/game/entities/templates/extended-sight.json
  - src/game/entities/index.ts
  - src/game/entities/templates/goblin.json
  - src/game/entities/templates/__tests__/firmware-templates.test.ts
autonomous: true
requirements: ["FIRM-07", "FIRM-08", "FIRM-09", "FIRM-11"]
---

# Phase 8 - Plan 04: Starter Firmware Abilities & Templates

<objective>
Create the three starter Firmware ability JSON entity templates (Phase_Shift.sh, Neural_Spike.exe, Extended_Sight.sys), register them in the entity pipeline, stub Firmware drops from enemies as Locked Files, and verify end-to-end ability activation with correct parameters.
</objective>

<task type="auto">
  <read_first>
    - src/game/entities/templates/goblin.json
    - src/game/entities/templates/player.json
    - src/shared/components/ability-def.ts
  </read_first>
  <action>
    Create three Firmware ability JSON entity templates in `src/game/entities/templates/`:

    1. `phase-shift.json`:
    ```json
    {
      "name": "phase_shift",
      "components": {
        "abilityDef": {
          "name": "Phase_Shift.sh",
          "heatCost": 25,
          "range": 0,
          "effectType": "dash",
          "damageAmount": 0,
          "dashDistance": 5,
          "visionRadius": 0,
          "heatPerTurn": 0,
          "isToggle": false,
          "isActive": false,
          "isLegacy": false
        }
      }
    }
    ```

    2. `neural-spike.json`:
    ```json
    {
      "name": "neural_spike",
      "components": {
        "abilityDef": {
          "name": "Neural_Spike.exe",
          "heatCost": 40,
          "range": 6,
          "effectType": "ranged_attack",
          "damageAmount": 15,
          "dashDistance": 0,
          "visionRadius": 0,
          "heatPerTurn": 0,
          "isToggle": false,
          "isActive": false,
          "isLegacy": false
        }
      }
    }
    ```

    3. `extended-sight.json`:
    ```json
    {
      "name": "extended_sight",
      "components": {
        "abilityDef": {
          "name": "Extended_Sight.sys",
          "heatCost": 10,
          "range": 0,
          "effectType": "toggle_vision",
          "damageAmount": 0,
          "dashDistance": 0,
          "visionRadius": 12,
          "heatPerTurn": 10,
          "isToggle": true,
          "isActive": false,
          "isLegacy": false
        }
      }
    }
    ```
  </action>
  <acceptance_criteria>
    - `test -f src/game/entities/templates/phase-shift.json` exits 0
    - `test -f src/game/entities/templates/neural-spike.json` exits 0
    - `test -f src/game/entities/templates/extended-sight.json` exits 0
    - `grep -q '"heatCost": 25' src/game/entities/templates/phase-shift.json` exits 0
    - `grep -q '"heatCost": 40' src/game/entities/templates/neural-spike.json` exits 0
    - `grep -q '"heatPerTurn": 10' src/game/entities/templates/extended-sight.json` exits 0
  </acceptance_criteria>
</task>

<task type="auto">
  <read_first>
    - src/game/entities/index.ts
    - src/engine/entity/registry.ts
  </read_first>
  <action>
    Update `src/game/entities/index.ts` (or wherever `registerGameTemplates()` is defined) to register the three new Firmware templates.

    Add imports for the JSON files:
    ```typescript
    import phaseShiftTemplate from './templates/phase-shift.json';
    import neuralSpikeTemplate from './templates/neural-spike.json';
    import extendedSightTemplate from './templates/extended-sight.json';
    ```

    Register them in `registerGameTemplates()`:
    ```typescript
    registry.register(phaseShiftTemplate);
    registry.register(neuralSpikeTemplate);
    registry.register(extendedSightTemplate);
    ```

    This follows the existing pattern used for `goblin`, `player`, and `health_potion` templates.
  </action>
  <acceptance_criteria>
    - `grep -q "phase_shift\|phaseShift" src/game/entities/index.ts` exits 0
    - `grep -q "neural_spike\|neuralSpike" src/game/entities/index.ts` exits 0
    - `grep -q "extended_sight\|extendedSight" src/game/entities/index.ts` exits 0
  </acceptance_criteria>
</task>

<task type="auto">
  <read_first>
    - src/game/entities/templates/goblin.json
  </read_first>
  <action>
    Stub Firmware drops from enemies (FIRM-08, FIRM-09).

    Modify `src/game/entities/templates/goblin.json` to add a rare Firmware drop to the loot table. Add to the existing `lootTable.drops` array:
    ```json
    { "template": "neural_spike", "chance": 0.05 }
    ```

    This represents a 5% chance for this Tier 1 enemy to drop a Firmware ability. In the full game, only Tier 2/3 enemies drop Firmware (FIRM-08), but for Phase 8 testing we add it to the only enemy type available. A TODO comment should note: "Phase 11 will restrict Firmware drops to Tier 2/3 enemies only."

    Note: The Locked File mechanic (FIRM-09) is stubbed — dropped Firmware entities are immediately usable. Phase 13 adds the compilation requirement. Add a code comment in the FirmwareSystem (or a TODO in the loot table section) noting this: "TODO(Phase 13): Firmware drops should require Flux compilation before equipping."
  </action>
  <acceptance_criteria>
    - `grep -q "neural_spike" src/game/entities/templates/goblin.json` exits 0
    - `grep -q "0.05" src/game/entities/templates/goblin.json` exits 0
  </acceptance_criteria>
</task>

<task type="tdd">
  <read_first>
    - src/shared/components/ability-def.ts
    - src/game/entities/templates/phase-shift.json
    - src/game/entities/templates/neural-spike.json
    - src/game/entities/templates/extended-sight.json
    - src/engine/entity/registry.ts
    - src/engine/entity/factory.ts
  </read_first>
  <action>
    Create `src/game/entities/templates/__tests__/firmware-templates.test.ts` with integration tests:

    1. **Template parsing**: Each JSON template validates against the AbilityDef Zod schema
       - Parse phase-shift.json → validate `AbilityDef.schema.parse(template.components.abilityDef)`
       - Parse neural-spike.json → validate same
       - Parse extended-sight.json → validate same

    2. **Entity creation**: Each template creates a valid entity via EntityFactory
       - Register all templates in a test EntityRegistry
       - Call `entityFactory.create(world, 'phase_shift', componentRegistry)` → entity has AbilityDef component
       - Verify created entity's AbilityDef data matches template values

    3. **Specific ability values**:
       - Phase_Shift: heatCost=25, effectType='dash', dashDistance=5
       - Neural_Spike: heatCost=40, effectType='ranged_attack', range=6, damageAmount=15
       - Extended_Sight: heatCost=10, effectType='toggle_vision', isToggle=true, heatPerTurn=10, visionRadius=12

    4. **Legacy code stub**: Setting `isLegacy=true` on an AbilityDef schema parses correctly with the field
  </action>
  <acceptance_criteria>
    - `test -f src/game/entities/templates/__tests__/firmware-templates.test.ts` exits 0
    - `npx vitest run src/game/entities/templates/__tests__/firmware-templates.test.ts` exits 0
  </acceptance_criteria>
</task>

<verification>
After completing all tasks:
1. `npx vitest run src/game/entities/templates/__tests__/firmware-templates.test.ts` — all template tests pass
2. `npx vitest run` — full suite passes (including all Wave 1/2/3 tests)
3. `npx tsc --noEmit` — TypeScript compiles clean
4. Manual: Start the game, verify the entity pipeline initializes without errors
</verification>

<must_haves>
- Phase_Shift.sh: 25 Heat cost, dash up to 5 tiles ignoring collision (FIRM-07)
- Neural_Spike.exe: 40 Heat cost, ranged attack at 6 range, 15 damage (FIRM-07)
- Extended_Sight.sys: 10 Heat/turn toggle, wall-penetrating vision radius 12 (FIRM-07)
- All templates registered in EntityRegistry and creatable via EntityFactory
- All templates validate against AbilityDef Zod schema
- Firmware drop stubbed on enemy loot table (FIRM-08)
- Locked File compilation deferred to Phase 13 with TODO (FIRM-09)
- isLegacy field present and defaults to false (FIRM-11 stub)
</must_haves>
